# Recipes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un bounded context `Recipes` qui permet de saisir des recettes en admin et de les afficher publiquement sous forme de grille de fusion, avec un multiplicateur de portions et un suivi d'avancement côté navigateur.

**Architecture:** Un arbre ingrédients/étapes stocké dans une colonne `jsonb` et mappé vers des value objects immuables par Valinor. Un `GridBuilder` pur transforme cet arbre en grille de cellules avec `rowspan` et `colspan`, rendue par un composant Twig. Le multiplicateur et le cochage sont deux contrôleurs Stimulus opérant sur des `data-*`, donc le serveur ne rend qu'une seule réponse cachable. L'édition passe par un composant Vue monté dans un champ EasyAdmin custom.

**Tech Stack:** PHP 8.5, Symfony 8.1, Doctrine ORM 3 / DBAL 4, PostgreSQL (`jsonb`), Valinor 2, EasyAdmin 5, Twig + UX TwigComponent, Stimulus, Vue 3, Vite via Symfony Reprise, Tailwind 4, PHPUnit 13, Foundry 2.

**Spec:** `docs/superpowers/specs/2026-08-31-recipes-design.md`

## Global Constraints

- PHP `~8.5.0`. Toute commande PHP passe par `symfony php` ou `symfony composer` — le binaire `php` local est en 8.4 et échoue sur le platform check.
- Tout fichier PHP commence par `<?php`, ligne vide, `declare(strict_types=1);`, ligne vide, `namespace …;`.
- `phpunit.xml.dist` active `requireCoverageMetadata="true"` et `beStrictAboutCoverageMetadata="true"`: **toute classe de test doit porter `#[CoversClass(…)]`**, et `#[UsesClass(…)]` pour chaque autre classe applicative touchée. Sans ça le test est marqué risky et la suite échoue (`failOnRisky="true"`).
- `executionOrder="random"`: aucun test ne peut dépendre de l'ordre.
- Les méthodes qui surchargent ou implémentent portent `#[\Override]`.
- Style: 4 espaces, virgule finale sur les listes multi-lignes, `final` par défaut sur les classes non étendues. `make cs.fix` fait autorité.
- `make qa` doit passer en fin de chaque tâche: `archi refactor cs lint phpstan test`.
- **Ne pas modifier `deptrac_domains.yaml`.** Le fichier ne couvre aucune classe aujourd'hui et le réparer suppose une PR à part (voir §6 de la spec). `deptrac_layers.yaml` couvre le nouveau contexte sans modification.
- **Ne jamais éditer une migration déjà jouée.** Tant que la migration de la tâche 3 n'a pas tourné, la corriger sur place est acceptable.
- Toutes les nouvelles routes publiques sont localisées via `#[Route(path: ['en' => …, 'fr' => …])]`, comme le reste du site.
- Branche de travail: `feat/recipes-bounded-context`, déjà créée.

---

### Task 1: Domaine — identifiant, enums, value objects

**Files:**

- Create: `src/Shared/Domain/Data/ValueObject/RecipeId.php`
- Create: `src/Shared/Infrastructure/Database/Doctrine/DBAL/Types/RecipeIdType.php`
- Create: `src/Recipes/Domain/Data/RecipeType.php`
- Create: `src/Recipes/Domain/Data/Unit.php`
- Create: `src/Recipes/Domain/Data/Route.php`
- Create: `src/Recipes/Domain/Data/Ingredient.php`
- Create: `src/Recipes/Domain/Data/Step.php`
- Create: `src/Recipes/Domain/Data/RecipeContent.php`
- Modify: `config/packages/doctrine.yaml` (bloc `dbal.types`)
- Test: `tests/Recipes/Domain/Data/RecipeTypeTest.php`
- Test: `tests/Recipes/Domain/Data/UnitTest.php`
- Test: `tests/Recipes/Domain/Data/StepTest.php`

**Interfaces:**

- Consumes: `App\Shared\Domain\Data\UuidTrait`, `App\Shared\Domain\Data\ValueObject\Id`, `App\Shared\Infrastructure\Database\Doctrine\DBAL\Types\AbstractIdType`.
- Produces: `RecipeId::generate()`, `RecipeType::{STARTER,MAIN,DESSERT}` avec `toTranslatable(): TranslatableMessage`, `Unit` (16 cas) avec `toTranslatable(float $count): TranslatableMessage`, `Route::{LIST,VIEW}`, et les trois value objects `Ingredient(string $id, string $label, ?float $min, ?float $max, ?Unit $unit)`, `Step(string $id, string $text, list<Step|Ingredient> $children)`, `RecipeContent(list<Step|Ingredient> $roots)`.

- [ ] **Step 1: Écrire les tests des enums et de l'invariant de `Step`**

`tests/Recipes/Domain/Data/RecipeTypeTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Domain\Data;

use App\Recipes\Domain\Data\RecipeType;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\TestCase;
use function Symfony\Component\Translation\t;

#[CoversClass(RecipeType::class)]
final class RecipeTypeTest extends TestCase
{
    public function testToTranslatable(): void
    {
        $this->assertEquals(t('recipe_type.starter'), RecipeType::STARTER->toTranslatable());
        $this->assertEquals(t('recipe_type.main'), RecipeType::MAIN->toTranslatable());
        $this->assertEquals(t('recipe_type.dessert'), RecipeType::DESSERT->toTranslatable());
    }
}
```

`tests/Recipes/Domain/Data/UnitTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Domain\Data;

use App\Recipes\Domain\Data\Unit;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\TestCase;
use function Symfony\Component\Translation\t;

#[CoversClass(Unit::class)]
final class UnitTest extends TestCase
{
    public function testToTranslatableCarriesTheCount(): void
    {
        $this->assertEquals(t('unit.tbsp', ['count' => 2.0]), Unit::TBSP->toTranslatable(2.0));
    }

    public function testEveryCaseIsBackedByItsLowercasedName(): void
    {
        foreach (Unit::cases() as $unit) {
            $this->assertSame(strtolower($unit->name), $unit->value);
        }
    }
}
```

`tests/Recipes/Domain/Data/StepTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Domain\Data;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\Step;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use PHPUnit\Framework\TestCase;

#[CoversClass(Step::class)]
#[UsesClass(Ingredient::class)]
final class StepTest extends TestCase
{
    public function testItRejectsAStepWithoutChildren(): void
    {
        $this->expectException(\InvalidArgumentException::class);
        $this->expectExceptionMessage('Step "s1" must have at least one child.');

        new Step('s1', 'Mélanger', []);
    }

    public function testItAcceptsIngredientsAndStepsAsChildren(): void
    {
        $ingredient = new Ingredient('i1', 'de farine', 200.0, null, null);
        $inner = new Step('s1', 'Tamiser', [$ingredient]);
        $outer = new Step('s2', 'Mélanger', [$inner, $ingredient]);

        $this->assertSame([$inner, $ingredient], $outer->children);
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `symfony php vendor/bin/phpunit tests/Recipes`
Expected: FAIL, `Class "App\Recipes\Domain\Data\RecipeType" not found`.

- [ ] **Step 3: Créer l'identifiant et son type DBAL**

`src/Shared/Domain/Data/ValueObject/RecipeId.php`:

```php
<?php

declare(strict_types=1);

namespace App\Shared\Domain\Data\ValueObject;

use App\Shared\Domain\Data\UuidTrait;

final class RecipeId implements Id
{
    use UuidTrait;
}
```

`src/Shared/Infrastructure/Database/Doctrine/DBAL/Types/RecipeIdType.php`:

```php
<?php

declare(strict_types=1);

namespace App\Shared\Infrastructure\Database\Doctrine\DBAL\Types;

use App\Shared\Domain\Data\ValueObject\RecipeId;

final class RecipeIdType extends AbstractIdType
{
    public const string NAME = 'recipe_id';

    #[\Override]
    public function getName(): string
    {
        return self::NAME;
    }

    #[\Override]
    protected function getIdClass(): string
    {
        return RecipeId::class;
    }
}
```

Dans `config/packages/doctrine.yaml`, ajouter dans `doctrine.dbal.types`, en respectant l'ordre alphabétique existant (entre `project_id` et `user_id`):

```yaml
recipe_id: App\Shared\Infrastructure\Database\Doctrine\DBAL\Types\RecipeIdType
```

- [ ] **Step 4: Créer les enums**

`src/Recipes/Domain/Data/RecipeType.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

use Symfony\Component\Translation\TranslatableMessage;
use function Symfony\Component\Translation\t;

enum RecipeType: string
{
    case STARTER = 'starter';

    case MAIN = 'main';

    case DESSERT = 'dessert';

    public function toTranslatable(): TranslatableMessage
    {
        return t('recipe_type.' . $this->value);
    }
}
```

`src/Recipes/Domain/Data/Unit.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

use Symfony\Component\Translation\TranslatableMessage;
use function Symfony\Component\Translation\t;

/**
 * Un ingrédient qui se compte sans unité ("2 oignons", "½ concombre") a `unit = null`,
 * il n'existe donc volontairement pas de cas "PIECE".
 */
enum Unit: string
{
    case G = 'g';

    case KG = 'kg';

    case ML = 'ml';

    case CL = 'cl';

    case L = 'l';

    case TBSP = 'tbsp';

    case TSP = 'tsp';

    case PINCH = 'pinch';

    case CLOVE = 'clove';

    case SLICE = 'slice';

    case BUNCH = 'bunch';

    case SPRIG = 'sprig';

    case HANDFUL = 'handful';

    case DROP = 'drop';

    case CAN = 'can';

    case PACK = 'pack';

    public function toTranslatable(float $count): TranslatableMessage
    {
        return t('unit.' . $this->value, [
            'count' => $count,
        ]);
    }
}
```

`src/Recipes/Domain/Data/Route.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

enum Route: string
{
    case LIST = 'app.recipes.list';

    case VIEW = 'app.recipes.view';
}
```

- [ ] **Step 5: Créer les value objects de l'arbre**

`src/Recipes/Domain/Data/Ingredient.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

final readonly class Ingredient
{
    public function __construct(
        public string $id,
        public string $label,
        public ?float $min = null,
        public ?float $max = null,
        public ?Unit $unit = null,
    ) {
    }
}
```

`src/Recipes/Domain/Data/Step.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

final readonly class Step
{
    /**
     * @param list<Step|Ingredient> $children
     */
    public function __construct(
        public string $id,
        public string $text,
        public array $children,
    ) {
        if ($children === []) {
            throw new \InvalidArgumentException(\sprintf('Step "%s" must have at least one child.', $id));
        }
    }
}
```

`src/Recipes/Domain/Data/RecipeContent.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

final readonly class RecipeContent
{
    /**
     * @param list<Step|Ingredient> $roots
     */
    public function __construct(
        public array $roots = [],
    ) {
    }
}
```

- [ ] **Step 6: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes`
Expected: PASS, 4 tests.

- [ ] **Step 7: Style et analyse statique**

Run: `make cs.fix && make phpstan && make archi`
Expected: aucune violation. `deptrac_layers.yaml` doit accepter `App\Recipes\Domain\` (il n'utilise que `TranslatableMessage`, déjà autorisé pour la layer `Domain`).

- [ ] **Step 8: Commit**

```bash
git add src/Shared src/Recipes config/packages/doctrine.yaml tests/Recipes
git commit -m "feat(recipes): ajoute l'identifiant, les enums et les value objects du domaine"
```

---

### Task 2: Mapping JSON — type DBAL `recipe_content`

**Files:**

- Create: `src/Recipes/Infrastructure/Doctrine/DBAL/Type/RecipeContentType.php`
- Modify: `config/packages/doctrine.yaml` (bloc `dbal.types`)
- Test: `tests/Recipes/Infrastructure/Doctrine/DBAL/Type/RecipeContentTypeTest.php`

**Interfaces:**

- Consumes: `RecipeContent`, `Step`, `Ingredient`, `Unit` de la tâche 1.
- Produces: `RecipeContentType::NAME = 'recipe_content'`, `convertToPHPValue(): ?RecipeContent`, `convertToDatabaseValue(): ?string`. C'est ce type qui est référencé par `#[ORM\Column(type: RecipeContentType::NAME)]` dans la tâche 3.

Le type est instancié par Doctrine sans injection de dépendances: il construit donc son propre `MapperBuilder` Valinor et le mémorise dans une propriété.

- [ ] **Step 1: Écrire le test d'aller-retour**

`tests/Recipes/Infrastructure/Doctrine/DBAL/Type/RecipeContentTypeTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\Doctrine\DBAL\Type;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;
use App\Recipes\Domain\Data\Unit;
use App\Recipes\Infrastructure\Doctrine\DBAL\Type\RecipeContentType;
use Doctrine\DBAL\Platforms\PostgreSQLPlatform;
use Doctrine\DBAL\Types\Exception\ValueNotConvertible;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use PHPUnit\Framework\TestCase;

#[CoversClass(RecipeContentType::class)]
#[UsesClass(RecipeContent::class)]
#[UsesClass(Step::class)]
#[UsesClass(Ingredient::class)]
final class RecipeContentTypeTest extends TestCase
{
    private RecipeContentType $type;

    private PostgreSQLPlatform $platform;

    #[\Override]
    protected function setUp(): void
    {
        $this->type = new RecipeContentType();
        $this->platform = new PostgreSQLPlatform();
    }

    public function testItDeclaresJsonb(): void
    {
        $this->assertSame('JSONB', $this->type->getSQLDeclaration([], $this->platform));
    }

    public function testItRoundTrips(): void
    {
        $content = new RecipeContent([
            new Step('s1', 'Mélanger farine de riz et eau', [
                new Ingredient('i1', 'de farine de riz gluant', 2.0, null, Unit::TBSP),
                new Ingredient('i2', "d'eau", 1.0, 2.0, Unit::TBSP),
            ]),
            new Ingredient('i3', 'concombre coupé en julienne', 0.5),
        ]);

        $json = $this->type->convertToDatabaseValue($content, $this->platform);
        $this->assertIsString($json);

        $restored = $this->type->convertToPHPValue($json, $this->platform);

        $this->assertEquals($content, $restored);
    }

    public function testItKeepsTheDiscriminatorInTheSerializedShape(): void
    {
        $content = new RecipeContent([new Ingredient('i1', 'de sel')]);

        $json = $this->type->convertToDatabaseValue($content, $this->platform);
        $decoded = json_decode((string) $json, true, 512, \JSON_THROW_ON_ERROR);

        $this->assertSame('ingredient', $decoded['roots'][0]['type']);
    }

    public function testNullRoundTripsToNull(): void
    {
        $this->assertNull($this->type->convertToDatabaseValue(null, $this->platform));
        $this->assertNull($this->type->convertToPHPValue(null, $this->platform));
    }

    public function testItRejectsMalformedJson(): void
    {
        $this->expectException(ValueNotConvertible::class);

        $this->type->convertToPHPValue('{"roots": [{"type": "step"}]}', $this->platform);
    }

    public function testItRejectsAStepWithoutChildren(): void
    {
        $this->expectException(ValueNotConvertible::class);

        $this->type->convertToPHPValue(
            '{"roots": [{"type": "step", "id": "s1", "text": "Mélanger", "children": []}]}',
            $this->platform,
        );
    }
}
```

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure`
Expected: FAIL, classe `RecipeContentType` introuvable.

- [ ] **Step 3: Implémenter le type DBAL**

`src/Recipes/Infrastructure/Doctrine/DBAL/Type/RecipeContentType.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Doctrine\DBAL\Type;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;
use CuyZ\Valinor\Mapper\MappingError;
use CuyZ\Valinor\Mapper\Source\Source;
use CuyZ\Valinor\Mapper\TreeMapper;
use CuyZ\Valinor\MapperBuilder;
use Doctrine\DBAL\Platforms\AbstractPlatform;
use Doctrine\DBAL\Types\Exception\InvalidType;
use Doctrine\DBAL\Types\Exception\ValueNotConvertible;
use Doctrine\DBAL\Types\Type;

final class RecipeContentType extends Type
{
    public const string NAME = 'recipe_content';

    private ?TreeMapper $mapper = null;

    #[\Override]
    public function getSQLDeclaration(array $column, AbstractPlatform $platform): string
    {
        return 'JSONB';
    }

    #[\Override]
    public function convertToPHPValue(mixed $value, AbstractPlatform $platform): ?RecipeContent
    {
        if ($value instanceof RecipeContent || $value === null) {
            return $value;
        }

        if (! \is_string($value)) {
            throw InvalidType::new($value, self::NAME, ['null', 'string', RecipeContent::class]);
        }

        try {
            return $this->mapper()
                ->map(RecipeContent::class, Source::json($value));
        } catch (MappingError | \InvalidArgumentException $error) {
            // \InvalidArgumentException couvre l'invariant du constructeur de Step
            // (une étape sans enfant), que Valinor laisse remonter tel quel.
            throw ValueNotConvertible::new($value, self::NAME, null, $error);
        }
    }

    #[\Override]
    public function convertToDatabaseValue(mixed $value, AbstractPlatform $platform): ?string
    {
        if ($value === null) {
            return null;
        }

        if (! $value instanceof RecipeContent) {
            throw InvalidType::new($value, self::NAME, ['null', RecipeContent::class]);
        }

        return json_encode([
            'roots' => array_map($this->normalizeNode(...), $value->roots),
        ], \JSON_THROW_ON_ERROR | \JSON_UNESCAPED_UNICODE | \JSON_UNESCAPED_SLASHES);
    }

    /**
     * @return array<string, mixed>
     */
    private function normalizeNode(Step|Ingredient $node): array
    {
        if ($node instanceof Ingredient) {
            return [
                'type' => 'ingredient',
                'id' => $node->id,
                'label' => $node->label,
                'min' => $node->min,
                'max' => $node->max,
                'unit' => $node->unit?->value,
            ];
        }

        return [
            'type' => 'step',
            'id' => $node->id,
            'text' => $node->text,
            'children' => array_map($this->normalizeNode(...), $node->children),
        ];
    }

    private function mapper(): TreeMapper
    {
        return $this->mapper ??= (new MapperBuilder())
            ->allowSuperfluousKeys()
            ->infer(
                Step::class . '|' . Ingredient::class,
                /** @param 'step'|'ingredient' $type */
                static fn (string $type): string => $type === 'step' ? Step::class : Ingredient::class,
            )
            ->mapper();
    }
}
```

**Note pour l'implémenteur:** l'API `infer()` de Valinor résout une union à partir d'une clé du tableau source. Si la signature exacte diffère dans la version installée (`cuyz/valinor` ^2.6), lire `vendor/cuyz/valinor/docs/` ou le README pour la forme attendue, et adapter — le contrat testé (`type` valant `step` ou `ingredient`) ne change pas. Si `infer()` ne convient pas, l'alternative est un `registerConstructor()` sur une méthode statique `RecipeContent::fromArray()`.

- [ ] **Step 4: Enregistrer le type**

Dans `config/packages/doctrine.yaml`, `doctrine.dbal.types`, en ordre alphabétique (avant `recipe_id`):

```yaml
recipe_content: App\Recipes\Infrastructure\Doctrine\DBAL\Type\RecipeContentType
```

- [ ] **Step 5: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes`
Expected: PASS.

- [ ] **Step 6: Style, analyse, architecture**

Run: `make cs.fix && make phpstan && make archi`

- [ ] **Step 7: Commit**

```bash
git add src/Recipes config/packages/doctrine.yaml tests/Recipes
git commit -m "feat(recipes): ajoute le type DBAL recipe_content adossé à Valinor"
```

---

### Task 3: Entité `Recipe`, repository, migration, factory

**Files:**

- Create: `src/Recipes/Domain/Data/Recipe.php`
- Create: `src/Recipes/Domain/Repository/RecipeRepository.php`
- Create: `src/Recipes/Infrastructure/Doctrine/Repository/RecipeORMRepository.php`
- Create: `src/Recipes/Infrastructure/Foundry/Factory/RecipeFactory.php`
- Create: `config/services/recipes.php`
- Create: `migrations/VersionYYYYMMDDHHMMSS.php` (généré)
- Modify: `config/packages/doctrine.yaml` (bloc `orm.mappings`)
- Test: `tests/Recipes/Domain/Data/RecipeTest.php`

**Interfaces:**

- Consumes: `RecipeId`, `RecipeType`, `RecipeContent`, `Route`, `RecipeContentType::NAME` des tâches 1 et 2.
- Produces: `Recipe` avec `getId(): RecipeId`, `getName(): ?string`, `getSlug(): ?string`, `getType(): RecipeType`, `getLocale(): Locale`, `getServings(): int`, `getContent(): RecipeContent`, `isVisible(): bool`, `getCreatedAt()/getUpdatedAt(): \DateTimeImmutable`, plus les setters fluides correspondants. `RecipeRepository` avec `findAllVisible(Locale $locale): array`, `findOneBySlug(string $slug): ?Recipe`, `findLatestUpdated(): ?Recipe`.

- [ ] **Step 1: Écrire le test de l'entité**

`tests/Recipes/Domain/Data/RecipeTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Domain\Data;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Route;
use App\Shared\Domain\HttpCache\CacheItem;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use PHPUnit\Framework\TestCase;

#[CoversClass(Recipe::class)]
#[UsesClass(RecipeContent::class)]
#[UsesClass(CacheItem::class)]
final class RecipeTest extends TestCase
{
    public function testItStartsEmptyAndHidden(): void
    {
        $recipe = new Recipe();

        $this->assertFalse($recipe->isVisible());
        $this->assertSame(4, $recipe->getServings());
        $this->assertSame([], $recipe->getContent()->roots);
    }

    public function testEtagIsScopedToTheContext(): void
    {
        $recipe = new Recipe();

        $this->assertStringStartsWith('recipes:recipe:' . $recipe->getId(), $recipe->getEtag());
    }

    public function testCacheItemsCoverTheListAndItsOwnPage(): void
    {
        $recipe = (new Recipe())->setSlug('nouilles-udon');

        $this->assertEquals([
            CacheItem::fromRoute(Route::LIST),
            CacheItem::fromRoute(Route::VIEW, ['slug' => 'nouilles-udon']),
        ], $recipe->getCacheItems());
    }
}
```

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Domain/Data/RecipeTest.php`
Expected: FAIL, classe `Recipe` introuvable.

- [ ] **Step 3: Vérifier la signature de `CacheItem::fromRoute()`**

Run: `cat src/Shared/Domain/HttpCache/CacheItem.php`
Adapter l'appel `fromRoute(Route::VIEW, ['slug' => …])` si le second paramètre n'a pas cette forme, et corriger le test en conséquence avant d'écrire l'entité.

- [ ] **Step 4: Écrire l'entité**

`src/Recipes/Domain/Data/Recipe.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Data;

use App\Shared\Domain\Data\Locale;
use App\Shared\Domain\Data\ValueObject\RecipeId;
use App\Shared\Domain\HttpCache\CacheableEntity;
use App\Shared\Domain\HttpCache\CacheItem;
use Doctrine\ORM\Mapping as ORM;
use Symfony\Component\Clock\Clock;
use Symfony\Component\Validator\Constraints as Assert;

#[ORM\Entity]
#[ORM\Table(name: 'recipe')]
#[ORM\Index(columns: ['locale', 'visible'])]
#[ORM\HasLifecycleCallbacks]
class Recipe implements CacheableEntity
{
    #[ORM\Id]
    #[ORM\GeneratedValue(strategy: 'NONE')]
    #[ORM\Column(type: 'recipe_id')]
    private RecipeId $id;

    #[ORM\Column(length: 255)]
    #[Assert\NotBlank]
    #[Assert\Length(min: 3, max: 255)]
    private ?string $name = null;

    #[ORM\Column(length: 255, unique: true)]
    #[Assert\NotBlank]
    #[Assert\Length(min: 3, max: 255)]
    private ?string $slug = null;

    #[ORM\Column]
    private RecipeType $type = RecipeType::MAIN;

    #[ORM\Column(length: 5, options: [
        'default' => Locale::EN,
    ])]
    private Locale $locale = Locale::EN;

    #[ORM\Column(type: 'smallint', options: [
        'default' => 4,
    ])]
    #[Assert\Positive]
    private int $servings = 4;

    #[ORM\Column(type: 'recipe_content')]
    private RecipeContent $content;

    #[ORM\Column(options: [
        'default' => false,
    ])]
    private bool $visible = false;

    #[ORM\Column]
    private \DateTimeImmutable $createdAt;

    #[ORM\Column]
    private \DateTimeImmutable $updatedAt;

    public function __construct()
    {
        $this->id = RecipeId::generate();
        $this->content = new RecipeContent();
        $this->createdAt = Clock::get()->now();
        $this->updatedAt = $this->createdAt;
    }

    public function getId(): RecipeId
    {
        return $this->id;
    }

    public function getName(): ?string
    {
        return $this->name;
    }

    public function setName(string $name): static
    {
        $this->name = $name;

        return $this;
    }

    public function getSlug(): ?string
    {
        return $this->slug;
    }

    public function setSlug(string $slug): static
    {
        $this->slug = $slug;

        return $this;
    }

    public function getType(): RecipeType
    {
        return $this->type;
    }

    public function setType(RecipeType $type): static
    {
        $this->type = $type;

        return $this;
    }

    public function getLocale(): Locale
    {
        return $this->locale;
    }

    public function setLocale(Locale $locale): static
    {
        $this->locale = $locale;

        return $this;
    }

    public function getServings(): int
    {
        return $this->servings;
    }

    public function setServings(int $servings): static
    {
        $this->servings = $servings;

        return $this;
    }

    public function getContent(): RecipeContent
    {
        return $this->content;
    }

    public function setContent(RecipeContent $content): static
    {
        $this->content = $content;

        return $this;
    }

    public function isVisible(): bool
    {
        return $this->visible;
    }

    public function setVisible(bool $visible): static
    {
        $this->visible = $visible;

        return $this;
    }

    public function getCreatedAt(): \DateTimeImmutable
    {
        return $this->createdAt;
    }

    public function getUpdatedAt(): \DateTimeImmutable
    {
        return $this->updatedAt;
    }

    #[ORM\PreUpdate]
    public function preUpdate(): void
    {
        $this->updatedAt = new \DateTimeImmutable();
    }

    #[\Override]
    public function getEtag(): string
    {
        return 'recipes:recipe:' . $this->id . ':' . $this->updatedAt->format('U');
    }

    #[\Override]
    public function getCacheItems(): array
    {
        return [
            CacheItem::fromRoute(Route::LIST),
            CacheItem::fromRoute(Route::VIEW, [
                'slug' => $this->slug,
            ]),
        ];
    }
}
```

- [ ] **Step 5: Écrire le repository**

`src/Recipes/Domain/Repository/RecipeRepository.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Domain\Repository;

use App\Recipes\Domain\Data\Recipe;
use App\Shared\Domain\Data\Locale;

interface RecipeRepository
{
    public function add(Recipe $recipe): void;

    /**
     * @return list<Recipe>
     */
    public function findAllVisible(Locale $locale): array;

    public function findOneBySlug(string $slug): ?Recipe;

    public function findLatestUpdated(): ?Recipe;
}
```

`src/Recipes/Infrastructure/Doctrine/Repository/RecipeORMRepository.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Doctrine\Repository;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Shared\Domain\Data\Locale;
use Doctrine\Bundle\DoctrineBundle\Repository\ServiceEntityRepository;
use Doctrine\Persistence\ManagerRegistry;

/**
 * @extends ServiceEntityRepository<Recipe>
 */
class RecipeORMRepository extends ServiceEntityRepository implements RecipeRepository
{
    public function __construct(ManagerRegistry $registry)
    {
        parent::__construct($registry, Recipe::class);
    }

    #[\Override]
    public function add(Recipe $recipe): void
    {
        $this->getEntityManager()
            ->persist($recipe);
    }

    #[\Override]
    public function findAllVisible(Locale $locale): array
    {
        return $this->createQueryBuilder('r')
            ->andWhere('r.visible = true')
            ->andWhere('r.locale = :locale')
            ->setParameter('locale', $locale)
            ->orderBy('r.type', 'ASC')
            ->addOrderBy('r.name', 'ASC')
            ->getQuery()
            ->getResult();
    }

    #[\Override]
    public function findOneBySlug(string $slug): ?Recipe
    {
        return $this->findOneBy([
            'slug' => $slug,
        ]);
    }

    #[\Override]
    public function findLatestUpdated(): ?Recipe
    {
        return $this->createQueryBuilder('r')
            ->orderBy('r.updatedAt', 'DESC')
            ->setMaxResults(1)
            ->getQuery()
            ->getOneOrNullResult();
    }
}
```

- [ ] **Step 6: Câbler le contexte dans le conteneur et dans Doctrine**

`config/services/recipes.php`, calqué sur `config/services/blog.php`:

```php
<?php

declare(strict_types=1);

namespace Symfony\Component\DependencyInjection\Loader\Configurator;

use App\Recipes\Domain\Repository\RecipeRepository;
use App\Recipes\Infrastructure\Doctrine\Repository\RecipeORMRepository;

return static function (ContainerConfigurator $containerConfigurator): void {
    $services = $containerConfigurator->services();

    $services->defaults()
        ->autowire()
        ->autoconfigure();

    $services->load('App\\Recipes\\', '../../src/Recipes')
        ->exclude([
            '../../src/Recipes/Domain/Data/**',
            '../../src/Recipes/Infrastructure/Doctrine/DBAL/Type/**',
            '../../src/Recipes/Infrastructure/Foundry/Factory/**',
        ]);

    if ($containerConfigurator->env() === 'dev' || $containerConfigurator->env() === 'test') {
        $services->load('App\\Recipes\\Infrastructure\\Foundry\\Factory\\', '../../src/Recipes/Infrastructure/Foundry/Factory');
    }

    $services->set(RecipeRepository::class)
        ->class(RecipeORMRepository::class);
};
```

Dans `config/packages/doctrine.yaml`, `doctrine.orm.mappings`, entre `Places` et `User`:

```yaml
Recipes:
    is_bundle: false
    type: attribute
    dir: "%kernel.project_dir%/src/Recipes/Domain"
    prefix: 'App\Recipes\Domain'
```

- [ ] **Step 7: Écrire la factory Foundry**

`src/Recipes/Infrastructure/Foundry/Factory/RecipeFactory.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Foundry\Factory;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\RecipeType;
use App\Recipes\Domain\Data\Step;
use App\Recipes\Domain\Data\Unit;
use App\Shared\Domain\Data\Locale;
use Zenstruck\Foundry\Persistence\PersistentProxyObjectFactory;

/**
 * @extends PersistentProxyObjectFactory<Recipe>
 */
final class RecipeFactory extends PersistentProxyObjectFactory
{
    #[\Override]
    public static function class(): string
    {
        return Recipe::class;
    }

    public function visible(): static
    {
        return $this->with([
            'visible' => true,
        ]);
    }

    public function french(): static
    {
        return $this->with([
            'locale' => Locale::FR,
        ]);
    }

    #[\Override]
    protected function defaults(): array
    {
        return [
            'content' => new RecipeContent([
                new Step('s1', 'Mélanger farine de riz et eau', [
                    new Ingredient('i1', 'de farine de riz gluant', 2.0, null, Unit::TBSP),
                    new Ingredient('i2', "d'eau", 1.0, 2.0, Unit::TBSP),
                ]),
            ]),
            'locale' => Locale::EN,
            'name' => self::faker()->words(3, true),
            'servings' => self::faker()->numberBetween(1, 8),
            'slug' => self::faker()->unique()->slug(),
            'type' => self::faker()->randomElement(RecipeType::cases()),
            'visible' => false,
        ];
    }
}
```

- [ ] **Step 8: Générer et relire la migration**

Run: `symfony console doctrine:migrations:diff --no-interaction`
Puis ouvrir le fichier généré dans `migrations/` et vérifier qu'il crée bien la table `recipe` avec `content JSONB NOT NULL`, l'index unique sur `slug` et l'index sur `(locale, visible)`. Supprimer tout `DROP`/`ALTER` sans rapport que le diff aurait pu ramasser.

Run: `symfony console doctrine:migrations:migrate --no-interaction`
Expected: migration jouée sans erreur.

- [ ] **Step 9: Lancer les tests et la validation de schéma**

Run: `symfony php vendor/bin/phpunit tests/Recipes && symfony console doctrine:schema:validate`
Expected: PASS, et le mapping est déclaré synchronisé avec la base.

- [ ] **Step 10: Style, analyse, architecture**

Run: `make cs.fix && make phpstan && make archi && make lint.back`

- [ ] **Step 11: Commit**

```bash
git add src/Recipes config/services/recipes.php config/packages/doctrine.yaml migrations tests/Recipes
git commit -m "feat(recipes): ajoute l'entité Recipe, son repository, sa migration et sa factory"
```

---

### Task 4: `GridBuilder` — l'algorithme de layout

**Files:**

- Create: `src/Recipes/Application/Grid/CellType.php`
- Create: `src/Recipes/Application/Grid/Cell.php`
- Create: `src/Recipes/Application/Grid/Row.php`
- Create: `src/Recipes/Application/Grid/Grid.php`
- Create: `src/Recipes/Application/Grid/GridBuilder.php`
- Test: `tests/Recipes/Application/Grid/GridBuilderTest.php`

**Interfaces:**

- Consumes: `RecipeContent`, `Step`, `Ingredient` de la tâche 1.
- Produces: `GridBuilder::__invoke(RecipeContent $content): Grid`. `Grid` a `public array $rows` (`list<Row>`) et `public int $columnCount`. `Row` a `public array $cells` (`list<Cell>`, triées par colonne croissante). `Cell` a `public CellType $type`, `public int $col`, `public int $rowSpan`, `public int $colSpan`, `public Step|Ingredient|null $node`, plus `isIngredient()`, `isStep()`, `isGap()` pour un usage confortable dans Twig.

C'est la pièce centrale du contexte. Les règles, tirées de §2 de la spec:

1. `col(ingrédient) = 0`, `col(étape) = 1 + max(col des enfants)`.
2. Les lignes sont les ingrédients dans l'ordre du parcours en profondeur **préfixe** des racines.
3. `rowSpan(nœud)` = nombre d'ingrédients dans son sous-arbre.
4. `rowStart(nœud)` = ligne de son premier ingrédient.
5. Un nœud en colonne `c` dont le parent est en colonne `p` produit une cellule vide de `colSpan = p - c - 1` si ce nombre est positif, à la colonne `c + 1`, avec le même `rowSpan`. Pour une racine, `p` vaut `columnCount`.

- [ ] **Step 1: Écrire le test avec la recette de référence**

`tests/Recipes/Application/Grid/GridBuilderTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Application\Grid;

use App\Recipes\Application\Grid\Cell;
use App\Recipes\Application\Grid\CellType;
use App\Recipes\Application\Grid\Grid;
use App\Recipes\Application\Grid\GridBuilder;
use App\Recipes\Application\Grid\Row;
use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use PHPUnit\Framework\TestCase;

#[CoversClass(GridBuilder::class)]
#[UsesClass(Grid::class)]
#[UsesClass(Row::class)]
#[UsesClass(Cell::class)]
#[UsesClass(RecipeContent::class)]
#[UsesClass(Step::class)]
#[UsesClass(Ingredient::class)]
final class GridBuilderTest extends TestCase
{
    public function testEmptyContentProducesAnEmptyGrid(): void
    {
        $grid = (new GridBuilder())(new RecipeContent());

        $this->assertSame([], $grid->rows);
        $this->assertSame(1, $grid->columnCount);
    }

    public function testASingleIngredientProducesOneRowAndOneColumn(): void
    {
        $grid = (new GridBuilder())(new RecipeContent([new Ingredient('i1', 'de sel')]));

        $this->assertCount(1, $grid->rows);
        $this->assertSame(1, $grid->columnCount);
        $this->assertCount(1, $grid->rows[0]->cells);
        $this->assertTrue($grid->rows[0]->cells[0]->isIngredient());
    }

    public function testASingleStepSpansItsIngredients(): void
    {
        $content = new RecipeContent([
            new Step('s1', 'Mélanger', [
                new Ingredient('i1', 'de farine'),
                new Ingredient('i2', "d'eau"),
            ]),
        ]);

        $grid = (new GridBuilder())($content);

        $this->assertSame(2, $grid->columnCount);
        $this->assertCount(2, $grid->rows);
        $this->assertSame([0, 1], array_map(static fn (Cell $c): int => $c->col, $grid->rows[0]->cells));
        $this->assertSame(2, $grid->rows[0]->cells[1]->rowSpan);
        $this->assertCount(1, $grid->rows[1]->cells);
    }

    public function testTheUdonRecipeMatchesTheReferenceLayout(): void
    {
        $grid = (new GridBuilder())(self::udon());

        $this->assertSame(7, $grid->columnCount);
        $this->assertCount(13, $grid->rows);

        // Chaque ligne totalise 7 colonnes une fois les rowSpan des lignes du dessus pris en compte.
        $occupied = array_fill(0, 13, 0);
        foreach ($grid->rows as $rowIndex => $row) {
            foreach ($row->cells as $cell) {
                for ($r = $rowIndex; $r < $rowIndex + $cell->rowSpan; ++$r) {
                    $occupied[$r] += $cell->colSpan;
                }
            }
        }
        $this->assertSame(array_fill(0, 13, 7), $occupied);
    }

    public function testTheUdonRecipePlacesEachStepInTheExpectedColumn(): void
    {
        $grid = (new GridBuilder())(self::udon());

        $expected = [
            's_beans' => ['col' => 1, 'rowStart' => 0, 'rowSpan' => 1],
            's_pork' => ['col' => 1, 'rowStart' => 1, 'rowSpan' => 3],
            's_veggies' => ['col' => 2, 'rowStart' => 1, 'rowSpan' => 5],
            's_paste' => ['col' => 3, 'rowStart' => 0, 'rowSpan' => 8],
            's_water' => ['col' => 4, 'rowStart' => 0, 'rowSpan' => 9],
            's_flour' => ['col' => 1, 'rowStart' => 9, 'rowSpan' => 2],
            's_thicken' => ['col' => 5, 'rowStart' => 0, 'rowSpan' => 11],
            's_noodles' => ['col' => 1, 'rowStart' => 11, 'rowSpan' => 1],
            's_serve' => ['col' => 6, 'rowStart' => 0, 'rowSpan' => 13],
        ];

        $actual = [];
        foreach ($grid->rows as $rowIndex => $row) {
            foreach ($row->cells as $cell) {
                if ($cell->isStep()) {
                    $actual[$cell->node->id] = [
                        'col' => $cell->col,
                        'rowStart' => $rowIndex,
                        'rowSpan' => $cell->rowSpan,
                    ];
                }
            }
        }

        // Les étapes sont découvertes dans l'ordre des lignes, pas dans celui de la recette:
        // trier les deux tableaux pour que la comparaison porte sur les valeurs et non sur l'ordre des clés.
        ksort($expected);
        ksort($actual);

        $this->assertSame($expected, $actual);
    }

    public function testTheUdonRecipeOrdersIngredientsByDepthFirstTraversal(): void
    {
        $grid = (new GridBuilder())(self::udon());

        $labels = [];
        foreach ($grid->rows as $row) {
            foreach ($row->cells as $cell) {
                if ($cell->isIngredient()) {
                    $labels[] = $cell->node->id;
                }
            }
        }

        $this->assertSame([
            'i_beans', 'i_pork', 'i_garlic', 'i_onion', 'i_zucchini', 'i_cabbage',
            'i_sugar', 'i_gochugaru', 'i_boiling_water', 'i_rice_flour', 'i_flour_water',
            'i_udon', 'i_cucumber',
        ], $labels);
    }

    public function testGapsFillTheDistanceToTheParentColumn(): void
    {
        $grid = (new GridBuilder())(self::udon());

        $gaps = [];
        foreach ($grid->rows as $rowIndex => $row) {
            foreach ($row->cells as $cell) {
                if ($cell->isGap()) {
                    $gaps[] = [$rowIndex, $cell->col, $cell->colSpan, $cell->rowSpan];
                }
            }
        }

        $this->assertContains([0, 2, 1, 1], $gaps, 'le trou à droite de "faire revenir la pâte"');
        $this->assertContains([9, 2, 3, 2], $gaps, 'le trou à droite de "mélanger farine de riz et eau"');
        $this->assertContains([11, 2, 4, 1], $gaps, 'le trou à droite de "cuire les nouilles"');
        $this->assertContains([12, 1, 5, 1], $gaps, 'le trou à droite du concombre');
    }

    private static function udon(): RecipeContent
    {
        return new RecipeContent([
            new Step('s_serve', 'Servir nouilles, sauce et concombre', [
                new Step('s_thicken', 'Verser mélange farine, laisser épaissir', [
                    new Step('s_water', 'Verser eau bouillante, mijoter jusqu\'à cuisson', [
                        new Step('s_paste', 'Ajouter pâte réservée, sucre et gochugaru', [
                            new Step('s_beans', 'Faire revenir la pâte de haricots noirs, 15 min', [
                                new Ingredient('i_beans', 'de pâte de haricots noirs'),
                            ]),
                            new Step('s_veggies', 'Ajouter courgette et chou, cuire 5 min', [
                                new Step('s_pork', 'Cuire le porc, ajouter ail et oignons, 5 min', [
                                    new Ingredient('i_pork', 'de poitrine de porc coupée en petits dés'),
                                    new Ingredient('i_garlic', "d'ail émincées"),
                                    new Ingredient('i_onion', 'oignons coupés en petits tronçons'),
                                ]),
                                new Ingredient('i_zucchini', 'courgette coupée en petits dés'),
                                new Ingredient('i_cabbage', 'chou coupé en petits tronçons'),
                            ]),
                            new Ingredient('i_sugar', 'de sucre'),
                            new Ingredient('i_gochugaru', 'de gochugaru'),
                        ]),
                        new Ingredient('i_boiling_water', "d'eau bouillante"),
                    ]),
                    new Step('s_flour', 'Mélanger farine de riz et eau', [
                        new Ingredient('i_rice_flour', 'de farine de riz gluant'),
                        new Ingredient('i_flour_water', "d'eau"),
                    ]),
                ]),
                new Step('s_noodles', 'Cuire nouilles Udon, égoutter', [
                    new Ingredient('i_udon', 'de nouilles Udon'),
                ]),
                new Ingredient('i_cucumber', 'concombre coupé en julienne'),
            ]),
        ]);
    }
}
```

- [ ] **Step 2: Lancer les tests et vérifier qu'ils échouent**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Application`
Expected: FAIL, classe `GridBuilder` introuvable.

- [ ] **Step 3: Écrire les objets de la grille**

`src/Recipes/Application/Grid/CellType.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Application\Grid;

enum CellType
{
    case INGREDIENT;

    case STEP;

    case GAP;
}
```

`src/Recipes/Application/Grid/Cell.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Application\Grid;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\Step;

final readonly class Cell
{
    public function __construct(
        public CellType $type,
        public int $col,
        public int $rowSpan,
        public int $colSpan,
        public Step|Ingredient|null $node = null,
    ) {
    }

    public function isIngredient(): bool
    {
        return $this->type === CellType::INGREDIENT;
    }

    public function isStep(): bool
    {
        return $this->type === CellType::STEP;
    }

    public function isGap(): bool
    {
        return $this->type === CellType::GAP;
    }
}
```

`src/Recipes/Application/Grid/Row.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Application\Grid;

final readonly class Row
{
    /**
     * @param list<Cell> $cells
     */
    public function __construct(
        public array $cells,
    ) {
    }
}
```

`src/Recipes/Application/Grid/Grid.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Application\Grid;

final readonly class Grid
{
    /**
     * @param list<Row> $rows
     */
    public function __construct(
        public array $rows,
        public int $columnCount,
    ) {
    }
}
```

- [ ] **Step 4: Écrire le builder**

`src/Recipes/Application/Grid/GridBuilder.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Application\Grid;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;

/**
 * Transforme l'arbre d'une recette en grille de cellules avec rowspan et colspan.
 *
 * La colonne d'une étape se calcule depuis les feuilles (1 + le max de ses enfants) et non
 * depuis la racine: une préparation courte reste ainsi collée à gauche même quand sa sortie
 * n'est consommée que très à droite.
 */
final readonly class GridBuilder
{
    public function __invoke(RecipeContent $content): Grid
    {
        /** @var array<string, array{col: int, rowStart: int, rowSpan: int}> $metrics */
        $metrics = [];
        $rowCount = 0;

        foreach ($content->roots as $root) {
            $this->measure($root, $metrics, $rowCount);
        }

        $columnCount = 1;
        foreach ($metrics as $metric) {
            $columnCount = max($columnCount, $metric['col'] + 1);
        }

        /** @var array<int, list<Cell>> $cellsByRow */
        $cellsByRow = array_fill(0, max($rowCount, 1), []);

        foreach ($content->roots as $root) {
            $this->place($root, $columnCount, $metrics, $cellsByRow);
        }

        $rows = [];
        for ($rowIndex = 0; $rowIndex < $rowCount; ++$rowIndex) {
            $cells = $cellsByRow[$rowIndex];
            usort($cells, static fn (Cell $a, Cell $b): int => $a->col <=> $b->col);
            $rows[] = new Row($cells);
        }

        return new Grid($rows, $columnCount);
    }

    /**
     * @param array<string, array{col: int, rowStart: int, rowSpan: int}> $metrics
     *
     * @return array{col: int, rowStart: int, rowSpan: int}
     */
    private function measure(Step|Ingredient $node, array &$metrics, int &$rowCount): array
    {
        $rowStart = $rowCount;

        if ($node instanceof Ingredient) {
            ++$rowCount;

            return $metrics[$node->id] = [
                'col' => 0,
                'rowStart' => $rowStart,
                'rowSpan' => 1,
            ];
        }

        $col = 0;
        foreach ($node->children as $child) {
            $childMetrics = $this->measure($child, $metrics, $rowCount);
            $col = max($col, $childMetrics['col'] + 1);
        }

        return $metrics[$node->id] = [
            'col' => $col,
            'rowStart' => $rowStart,
            'rowSpan' => $rowCount - $rowStart,
        ];
    }

    /**
     * @param array<string, array{col: int, rowStart: int, rowSpan: int}> $metrics
     * @param array<int, list<Cell>>                                      $cellsByRow
     */
    private function place(Step|Ingredient $node, int $parentCol, array $metrics, array &$cellsByRow): void
    {
        $metric = $metrics[$node->id];

        $cellsByRow[$metric['rowStart']][] = new Cell(
            $node instanceof Ingredient ? CellType::INGREDIENT : CellType::STEP,
            $metric['col'],
            $metric['rowSpan'],
            1,
            $node,
        );

        $gap = $parentCol - $metric['col'] - 1;
        if ($gap > 0) {
            $cellsByRow[$metric['rowStart']][] = new Cell(
                CellType::GAP,
                $metric['col'] + 1,
                $metric['rowSpan'],
                $gap,
            );
        }

        if ($node instanceof Step) {
            foreach ($node->children as $child) {
                $this->place($child, $metric['col'], $metrics, $cellsByRow);
            }
        }
    }
}
```

- [ ] **Step 5: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Application`
Expected: PASS, 7 tests. Si `testTheUdonRecipeMatchesTheReferenceLayout` échoue, c'est que la somme des `colSpan` par ligne ne fait pas 7: relire la règle 5 avant de toucher au reste.

- [ ] **Step 6: Style, analyse, architecture**

Run: `make cs.fix && make phpstan && make archi`
Expected: `deptrac_layers.yaml` accepte `Application -> Domain`.

- [ ] **Step 7: Commit**

```bash
git add src/Recipes/Application tests/Recipes/Application
git commit -m "feat(recipes): ajoute le GridBuilder qui aplatit l'arbre en grille rowspan/colspan"
```

---

### Task 5: `QuantityFormatter`

**Files:**

- Create: `src/Recipes/Application/QuantityFormatter.php`
- Test: `tests/Recipes/Application/QuantityFormatterTest.php`

**Interfaces:**

- Produces: `QuantityFormatter::format(float $value, string $decimalSeparator = ','): string`.

Le contrat, tiré de §3.4 de la spec: une valeur entière est rendue telle quelle; une partie fractionnaire proche à 0,01 près de ⅛ ¼ ⅓ ⅜ ½ ⅝ ⅔ ¾ ⅞ devient le glyphe correspondant, précédé de la partie entière si elle est non nulle; sinon arrondi à deux décimales sans zéros inutiles.

Le contrôleur Stimulus de la tâche 11 réimplémente exactement ces règles en JavaScript. Les deux jeux de cas doivent rester identiques.

- [ ] **Step 1: Écrire le test**

`tests/Recipes/Application/QuantityFormatterTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Application;

use App\Recipes\Application\QuantityFormatter;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\DataProvider;
use PHPUnit\Framework\TestCase;

#[CoversClass(QuantityFormatter::class)]
final class QuantityFormatterTest extends TestCase
{
    /**
     * @return iterable<string, array{float, string}>
     */
    public static function provideValues(): iterable
    {
        yield 'entier' => [4.0, '4'];
        yield 'entier large' => [500.0, '500'];
        yield 'zéro' => [0.0, '0'];
        yield 'un demi' => [0.5, '½'];
        yield 'un tiers' => [1 / 3, '⅓'];
        yield 'deux tiers' => [2 / 3, '⅔'];
        yield 'un quart' => [0.25, '¼'];
        yield 'trois quarts' => [0.75, '¾'];
        yield 'un huitième' => [0.125, '⅛'];
        yield 'un et demi' => [1.5, '1 ½'];
        yield 'deux et un quart' => [2.25, '2 ¼'];
        yield 'décimale sans fraction connue' => [1.4, '1,4'];
        yield 'décimale loin de toute fraction' => [0.45, '0,45'];
        yield 'décimale happée par la fraction la plus proche' => [0.123456, '⅛'];
    }

    #[DataProvider('provideValues')]
    public function testFormat(float $value, string $expected): void
    {
        $this->assertSame($expected, (new QuantityFormatter())->format($value));
    }

    public function testTheDecimalSeparatorIsConfigurable(): void
    {
        $this->assertSame('1.4', (new QuantityFormatter())->format(1.4, '.'));
    }

    public function testMultiplyingAHalfByThreeGivesOneAndAHalf(): void
    {
        $this->assertSame('1 ½', (new QuantityFormatter())->format(0.5 * 3));
    }
}
```

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Application/QuantityFormatterTest.php`
Expected: FAIL, classe introuvable.

- [ ] **Step 3: Implémenter le formateur**

`src/Recipes/Application/QuantityFormatter.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Application;

final readonly class QuantityFormatter
{
    private const float TOLERANCE = 0.01;

    /**
     * Les clés d'un tableau PHP ne peuvent pas être des flottants, d'où la liste de paires.
     *
     * @var list<array{float, string}>
     */
    private const array FRACTIONS = [
        [0.125, '⅛'],
        [0.25, '¼'],
        [1 / 3, '⅓'],
        [0.375, '⅜'],
        [0.5, '½'],
        [0.625, '⅝'],
        [2 / 3, '⅔'],
        [0.75, '¾'],
        [0.875, '⅞'],
    ];

    public function format(float $value, string $decimalSeparator = ','): string
    {
        $whole = (int) floor($value);
        $fraction = $value - $whole;

        if ($fraction < self::TOLERANCE || $fraction > 1 - self::TOLERANCE) {
            return (string) (int) round($value);
        }

        foreach (self::FRACTIONS as [$decimal, $glyph]) {
            if (abs($fraction - $decimal) < self::TOLERANCE) {
                return $whole > 0 ? $whole . ' ' . $glyph : $glyph;
            }
        }

        $formatted = number_format($value, 2, $decimalSeparator, '');

        return rtrim(rtrim($formatted, '0'), $decimalSeparator);
    }
}
```

- [ ] **Step 4: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Application/QuantityFormatterTest.php`
Expected: PASS, 16 tests (14 lignes de data provider plus 2 tests dédiés).

- [ ] **Step 5: Style, analyse**

Run: `make cs.fix && make phpstan`

- [ ] **Step 6: Commit**

```bash
git add src/Recipes/Application tests/Recipes/Application
git commit -m "feat(recipes): ajoute le formateur de quantités avec fractions vulgaires"
```

---

### Task 6: Catalogue de traductions

**Files:**

- Modify: `translations/messages+intl-icu.en.yaml`
- Modify: `translations/messages+intl-icu.fr.yaml`

**Interfaces:**

- Consumes: les clés produites par `RecipeType::toTranslatable()` et `Unit::toTranslatable()` de la tâche 1.
- Produces: les clés `recipe_type.*`, `unit.*`, `quantity.range`, `recipes.*`, `nav.recipes`, consommées par les tâches 7 à 10 et 15.

Le domaine est `messages+intl-icu`, donc **toutes** les valeurs sont interprétées en ICU: une accolade littérale devrait être échappée, ce qui n'arrive dans aucune des chaînes ci-dessous.

- [ ] **Step 1: Ajouter les clés anglaises**

Dans `translations/messages+intl-icu.en.yaml`, en respectant l'ordre alphabétique des blocs de premier niveau déjà en place:

```yaml
nav:
    recipes: Recipes

quantity:
    range: "{min} to {max}"

recipe_type:
    starter: Starter
    main: Main course
    dessert: Dessert

recipes:
    title: Recipes
    ingredients: Ingredients
    steps: Steps
    servings: "For {count, plural, one {# person} other {# people}}"
    servings_label: Number of people
    decrease: One fewer person
    increase: One more person
    reset: Reset progress
    empty: No recipe yet.

unit:
    g: g
    kg: kg
    ml: ml
    cl: cl
    l: l
    tbsp: "{count, plural, one {tablespoon} other {tablespoons}}"
    tsp: "{count, plural, one {teaspoon} other {teaspoons}}"
    pinch: "{count, plural, one {pinch} other {pinches}}"
    clove: "{count, plural, one {clove} other {cloves}}"
    slice: "{count, plural, one {slice} other {slices}}"
    bunch: "{count, plural, one {bunch} other {bunches}}"
    sprig: "{count, plural, one {sprig} other {sprigs}}"
    handful: "{count, plural, one {handful} other {handfuls}}"
    drop: "{count, plural, one {drop} other {drops}}"
    can: "{count, plural, one {can} other {cans}}"
    pack: "{count, plural, one {pack} other {packs}}"
```

- [ ] **Step 2: Ajouter les clés françaises**

Dans `translations/messages+intl-icu.fr.yaml`, aux mêmes emplacements:

```yaml
nav:
    recipes: Recettes

quantity:
    range: "{min} à {max}"

recipe_type:
    starter: Entrée
    main: Plat
    dessert: Dessert

recipes:
    title: Recettes
    ingredients: Ingrédients
    steps: Étapes
    servings: "Pour {count, plural, one {# personne} other {# personnes}}"
    servings_label: Nombre de personnes
    decrease: Une personne de moins
    increase: Une personne de plus
    reset: Réinitialiser la progression
    empty: Aucune recette pour le moment.

unit:
    g: g
    kg: kg
    ml: ml
    cl: cl
    l: l
    tbsp: "{count, plural, one {cuillère à soupe} other {cuillères à soupe}}"
    tsp: "{count, plural, one {cuillère à café} other {cuillères à café}}"
    pinch: "{count, plural, one {pincée} other {pincées}}"
    clove: "{count, plural, one {gousse} other {gousses}}"
    slice: "{count, plural, one {tranche} other {tranches}}"
    bunch: "{count, plural, one {bouquet} other {bouquets}}"
    sprig: "{count, plural, one {brin} other {brins}}"
    handful: "{count, plural, one {poignée} other {poignées}}"
    drop: "{count, plural, one {goutte} other {gouttes}}"
    can: "{count, plural, one {boîte} other {boîtes}}"
    pack: "{count, plural, one {sachet} other {sachets}}"
```

- [ ] **Step 3: Vérifier que les deux catalogues sont complets et valides**

Run: `make lint.back && symfony console debug:translation en --only-missing && symfony console debug:translation fr --only-missing`
Expected: YAML valide, et aucune des clés ci-dessus listée comme manquante dans l'une des deux langues.

- [ ] **Step 4: Vérifier le rendu ICU des unités**

Run: `symfony console debug:translation fr --domain=messages+intl-icu | grep -c 'unit\.'`
Expected: 16 lignes, une par cas de l'enum `Unit`.

- [ ] **Step 5: Commit**

```bash
git add translations
git commit -m "feat(recipes): ajoute les traductions du contexte, unités au pluriel ICU comprises"
```

---

### Task 7: Composant Twig et grille desktop

**Files:**

- Create: `src/Recipes/Infrastructure/Twig/Components/Grid.php`
- Create: `templates/components/Recipe/Grid.html.twig`
- Modify: `config/packages/twig_component.yaml`
- Test: `tests/Recipes/Infrastructure/Twig/Components/GridTest.php`

**Interfaces:**

- Consumes: `Recipe` (tâche 3), `GridBuilder` et `Grid` (tâche 4), `QuantityFormatter` (tâche 5), les clés de traduction (tâche 6).
- Produces: le composant `<twig:Recipe:Grid :recipe="recipe" />`, qui expose `getGrid(): GridModel` et `formatQuantity(Ingredient $ingredient): string` au template.

La classe du composant s'appelle `Grid` mais consomme `App\Recipes\Application\Grid\Grid`: importer ce dernier sous l'alias `GridModel` pour éviter la collision.

- [ ] **Step 1: Écrire le test de rendu**

`tests/Recipes/Infrastructure/Twig/Components/GridTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\Twig\Components;

use App\Recipes\Application\Grid\Cell;
use App\Recipes\Application\Grid\Grid as GridModel;
use App\Recipes\Application\Grid\GridBuilder;
use App\Recipes\Application\Grid\Row;
use App\Recipes\Application\QuantityFormatter;
use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;
use App\Recipes\Domain\Data\Unit;
use App\Recipes\Infrastructure\Twig\Components\Grid;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use Symfony\Bundle\FrameworkBundle\Test\KernelTestCase;
use Twig\Environment;

#[CoversClass(Grid::class)]
#[UsesClass(GridBuilder::class)]
#[UsesClass(GridModel::class)]
#[UsesClass(Row::class)]
#[UsesClass(Cell::class)]
#[UsesClass(QuantityFormatter::class)]
#[UsesClass(Recipe::class)]
#[UsesClass(RecipeContent::class)]
#[UsesClass(Step::class)]
#[UsesClass(Ingredient::class)]
final class GridTest extends KernelTestCase
{
    #[\Override]
    protected function tearDown(): void
    {
        self::ensureKernelShutdown();
    }

    public function testItRendersRowSpansAndQuantities(): void
    {
        self::bootKernel();

        $recipe = (new Recipe())
            ->setSlug('nouilles-udon')
            ->setServings(4)
            ->setContent(new RecipeContent([
                new Step('s1', 'Mélanger farine et eau', [
                    new Ingredient('i1', 'de farine de riz gluant', 2.0, null, Unit::TBSP),
                    new Ingredient('i2', 'concombre en julienne', 0.5),
                ]),
            ]));

        /** @var Environment $twig */
        $twig = self::getContainer()->get('twig');
        $html = $twig->createTemplate('<twig:Recipe:Grid :recipe="recipe" />')
            ->render([
                'recipe' => $recipe,
            ]);

        $this->assertStringContainsString('rowspan="2"', $html);
        $this->assertStringContainsString('data-node-id="i1"', $html);
        $this->assertStringContainsString('data-qty-min="2"', $html);
        $this->assertStringContainsString('cuillères à soupe', $html);
        $this->assertStringContainsString('½', $html);
    }
}
```

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/Twig`
Expected: FAIL, composant `Recipe:Grid` introuvable.

- [ ] **Step 3: Déclarer le namespace du composant**

Dans `config/packages/twig_component.yaml`, sous `twig_component.defaults`:

```yaml
App\Recipes\Infrastructure\Twig\Components\:
    template_directory: "components/Recipe/"
    name_prefix: Recipe
```

- [ ] **Step 4: Écrire la classe du composant**

`src/Recipes/Infrastructure/Twig/Components/Grid.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Twig\Components;

use App\Recipes\Application\Grid\Grid as GridModel;
use App\Recipes\Application\Grid\GridBuilder;
use App\Recipes\Application\QuantityFormatter;
use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\Recipe;
use Symfony\UX\TwigComponent\Attribute\AsTwigComponent;

#[AsTwigComponent]
final class Grid
{
    public Recipe $recipe;

    public function __construct(
        private readonly GridBuilder $gridBuilder,
        private readonly QuantityFormatter $quantityFormatter,
    ) {
    }

    public function getGrid(): GridModel
    {
        return ($this->gridBuilder)($this->recipe->getContent());
    }

    public function formatQuantity(?float $value): ?string
    {
        return $value === null ? null : $this->quantityFormatter->format($value);
    }

    /**
     * Renvoie l'unité sous forme de TranslatableMessage portant le compte voulu, à passer
     * au filtre `trans` dans le template.
     *
     * Le compte doit être fixé ICI et pas dans le template: le filtre `trans` appliqué à un
     * TranslatableInterface ignore ses arguments et n'utilise que les paramètres déjà portés
     * par le message. `{{ message|trans({ count: 3 }) }}` rendrait donc toujours le pluriel
     * figé à la construction.
     */
    public function unitFor(Ingredient $ingredient, float $count): ?TranslatableMessage
    {
        return $ingredient->unit?->toTranslatable($count);
    }
}
```

Ajouter l'import `use Symfony\Component\Translation\TranslatableMessage;`.

- [ ] **Step 5: Écrire le template de la grille**

`templates/components/Recipe/Grid.html.twig`:

```twig
{% set grid = this.grid %}

<div class="hidden overflow-x-auto md:block">
    <table class="w-full border-collapse text-sm">
        <tbody>
            {% for row in grid.rows %}
                <tr>
                    {% for cell in row.cells %}
                        {% if cell.isGap %}
                            <td colspan="{{ cell.colSpan }}" rowspan="{{ cell.rowSpan }}" class="border border-transparent"></td>
                        {% elseif cell.isIngredient %}
                            {% set count = cell.node.max ?? cell.node.min ?? 0 %}
                            <td rowspan="{{ cell.rowSpan }}"
                                class="border border-neutral-200 px-3 py-2 align-middle dark:border-neutral-700"
                                data-node-id="{{ cell.node.id }}"
                                {% if cell.node.min is not null %}data-qty-min="{{ cell.node.min }}"{% endif %}
                                {% if cell.node.max is not null %}data-qty-max="{{ cell.node.max }}"{% endif %}
                                {% if cell.node.unit %}data-unit-one="{{ this.unitFor(cell.node, 1)|trans }}" data-unit-other="{{ this.unitFor(cell.node, 2)|trans }}"{% endif %}
                                data-action="click->recipe-progress#toggle"
                            >
                                {% if cell.node.min is not null %}
                                    <strong data-recipe-servings-target="quantity" class="font-mono">
                                        {%- if cell.node.max is not null -%}
                                            {{ 'quantity.range'|trans({ min: this.formatQuantity(cell.node.min), max: this.formatQuantity(cell.node.max) }) }}
                                        {%- else -%}
                                            {{ this.formatQuantity(cell.node.min) }}
                                        {%- endif -%}
                                        {%- if cell.node.unit %} {{ this.unitFor(cell.node, count)|trans }}{% endif -%}
                                    </strong>
                                {% endif %}
                                {{ cell.node.label }}
                            </td>
                        {% else %}
                            <td rowspan="{{ cell.rowSpan }}"
                                class="border border-neutral-200 bg-amber-950/30 px-3 py-2 text-center font-semibold align-middle dark:border-neutral-700"
                                data-node-id="{{ cell.node.id }}"
                                data-action="click->recipe-progress#toggle"
                            >
                                {{ cell.node.text }}
                            </td>
                        {% endif %}
                    {% endfor %}
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
```

- [ ] **Step 6: Lancer le test et vérifier qu'il passe**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/Twig`
Expected: PASS.

Si `data-qty-min="2"` ne matche pas parce que Twig rend `2` en flottant (`2.0`), ajuster le template avec un filtre explicite plutôt que l'assertion: le contrôleur JS fait un `Number()` et doit recevoir une valeur parsable.

- [ ] **Step 7: Style et lint**

Run: `make cs.fix && make lint.back && make phpstan`
Expected: `twig-cs-fixer` et `lint:twig` passent.

- [ ] **Step 8: Commit**

```bash
git add src/Recipes/Infrastructure/Twig templates/components/Recipe config/packages/twig_component.yaml tests/Recipes/Infrastructure/Twig
git commit -m "feat(recipes): ajoute le composant Twig qui rend la grille de fusion"
```

---

### Task 8: Rendu mobile en liste

**Files:**

- Modify: `templates/components/Recipe/Grid.html.twig`
- Modify: `src/Recipes/Infrastructure/Twig/Components/Grid.php`
- Modify: `tests/Recipes/Infrastructure/Twig/Components/GridTest.php`

**Interfaces:**

- Produces: `Grid::getFlatIngredients(): list<Ingredient>` et `Grid::getFlatSteps(): list<Step>`, cette dernière renvoyant les étapes dans l'ordre du parcours **postfixe**, qui est un ordre d'exécution valide.

Sous le point de rupture `md`, la grille est remplacée par deux listes. Les deux rendus coexistent dans le DOM et partagent les mêmes `data-node-id`, ce qui permet au contrôleur de progression de la tâche 12 de piloter les deux d'un coup.

- [ ] **Step 1: Ajouter le test du rendu mobile**

Ajouter à `tests/Recipes/Infrastructure/Twig/Components/GridTest.php`:

```php
    public function testItAlsoRendersAFlatListForSmallScreens(): void
    {
        self::bootKernel();

        $recipe = (new Recipe())
            ->setSlug('nouilles-udon')
            ->setContent(new RecipeContent([
                new Step('outer', 'Servir', [
                    new Step('inner', 'Mélanger farine et eau', [
                        new Ingredient('i1', 'de farine de riz gluant', 2.0, null, Unit::TBSP),
                    ]),
                    new Ingredient('i2', 'concombre en julienne', 0.5),
                ]),
            ]));

        /** @var Environment $twig */
        $twig = self::getContainer()->get('twig');
        $html = $twig->createTemplate('<twig:Recipe:Grid :recipe="recipe" />')
            ->render([
                'recipe' => $recipe,
            ]);

        $this->assertStringContainsString('md:hidden', $html);
        // Ordre postfixe: l'étape interne est listée avant celle qui la consomme.
        $this->assertLessThan(strpos($html, 'Servir</'), strpos($html, 'Mélanger farine et eau</'));
    }
```

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/Twig`
Expected: FAIL, `md:hidden` absent du rendu.

- [ ] **Step 3: Exposer les listes aplaties**

Ajouter à `src/Recipes/Infrastructure/Twig/Components/Grid.php`:

```php
    /**
     * @return list<Ingredient>
     */
    public function getFlatIngredients(): array
    {
        $ingredients = [];
        foreach ($this->getGrid()->rows as $row) {
            foreach ($row->cells as $cell) {
                if ($cell->isIngredient()) {
                    $ingredients[] = $cell->node;
                }
            }
        }

        return $ingredients;
    }

    /**
     * Ordre postfixe: une étape apparaît après celles dont elle consomme la sortie,
     * ce qui en fait un ordre d'exécution valide.
     *
     * @return list<Step>
     */
    public function getFlatSteps(): array
    {
        $steps = [];
        foreach ($this->recipe->getContent()->roots as $root) {
            $this->collectSteps($root, $steps);
        }

        return $steps;
    }

    /**
     * @param list<Step> $steps
     */
    private function collectSteps(Step|Ingredient $node, array &$steps): void
    {
        if (! $node instanceof Step) {
            return;
        }

        foreach ($node->children as $child) {
            $this->collectSteps($child, $steps);
        }

        $steps[] = $node;
    }
```

Ajouter l'import `use App\Recipes\Domain\Data\Step;` en tête de fichier.

- [ ] **Step 4: Ajouter le bloc mobile au template**

À la fin de `templates/components/Recipe/Grid.html.twig`:

```twig
<div class="md:hidden">
    <h2 class="mt-6 mb-2 text-lg font-semibold">{{ 'recipes.ingredients'|trans }}</h2>
    <ul class="space-y-1">
        {% for ingredient in this.flatIngredients %}
            {% set count = ingredient.max ?? ingredient.min ?? 0 %}
            <li class="border-b border-neutral-200 py-2 dark:border-neutral-700"
                data-node-id="{{ ingredient.id }}"
                {% if ingredient.min is not null %}data-qty-min="{{ ingredient.min }}"{% endif %}
                {% if ingredient.max is not null %}data-qty-max="{{ ingredient.max }}"{% endif %}
                {% if ingredient.unit %}data-unit-one="{{ this.unitFor(ingredient, 1)|trans }}" data-unit-other="{{ this.unitFor(ingredient, 2)|trans }}"{% endif %}
                role="button"
                tabindex="0"
                aria-pressed="false"
                data-action="click->recipe-progress#toggle keydown->recipe-progress#toggleOnKey"
            >
                {% if ingredient.min is not null %}
                    <strong data-recipe-servings-target="quantity" class="font-mono">
                        {%- if ingredient.max is not null -%}
                            {{ 'quantity.range'|trans({ min: this.formatQuantity(ingredient.min), max: this.formatQuantity(ingredient.max) }) }}
                        {%- else -%}
                            {{ this.formatQuantity(ingredient.min) }}
                        {%- endif -%}
                        {%- if ingredient.unit %} {{ this.unitFor(ingredient, count)|trans }}{% endif -%}
                    </strong>
                {% endif %}
                {{ ingredient.label }}
            </li>
        {% endfor %}
    </ul>

    <h2 class="mt-6 mb-2 text-lg font-semibold">{{ 'recipes.steps'|trans }}</h2>
    <ol class="list-decimal space-y-2 pl-5">
        {% for step in this.flatSteps %}
            <li data-node-id="{{ step.id }}"
                role="button"
                tabindex="0"
                aria-pressed="false"
                data-action="click->recipe-progress#toggle keydown->recipe-progress#toggleOnKey">{{ step.text }}</li>
        {% endfor %}
    </ol>
</div>
```

- [ ] **Step 5: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/Twig`
Expected: PASS, 2 tests.

- [ ] **Step 6: Style et lint**

Run: `make cs.fix && make lint.back && make phpstan`

- [ ] **Step 7: Commit**

```bash
git add src/Recipes/Infrastructure/Twig templates/components/Recipe tests/Recipes/Infrastructure/Twig
git commit -m "feat(recipes): ajoute le repli mobile en listes ingrédients puis étapes"
```

---

### Task 9: Contrôleurs publics et pages

**Files:**

- Create: `src/Recipes/Infrastructure/Symfony/Controller/ListController.php`
- Create: `src/Recipes/Infrastructure/Symfony/Controller/ViewController.php`
- Create: `templates/recipes/list.html.twig`
- Create: `templates/recipes/view.html.twig`
- Create: `tests/Recipes/Infrastructure/Double/Repository/FakeRecipeRepository.php`
- Test: `tests/Recipes/Infrastructure/Symfony/Controller/ListControllerTest.php`
- Test: `tests/Recipes/Infrastructure/Symfony/Controller/ViewControllerTest.php`

**Interfaces:**

- Consumes: `RecipeRepository` (tâche 3), `Route::{LIST,VIEW}` (tâche 1), le composant `Recipe:Grid` (tâches 7 et 8).
- Produces: les routes `app.recipes.list` (`/recipes`, `/fr/recettes`) et `app.recipes.view` (`/recipes/{slug}`, `/fr/recettes/{slug}`).

Le double de test suit `tests/Places/Infrastructure/Double/Repository/FakePlaceRepository.php`: lire ce fichier avant d'écrire le sien pour en reprendre la forme exacte.

- [ ] **Step 1: Écrire le double de repository**

`tests/Recipes/Infrastructure/Double/Repository/FakeRecipeRepository.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\Double\Repository;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Shared\Domain\Data\Locale;

final class FakeRecipeRepository implements RecipeRepository
{
    /**
     * @var list<Recipe>
     */
    private array $recipes = [];

    #[\Override]
    public function add(Recipe $recipe): void
    {
        $this->recipes[] = $recipe;
    }

    #[\Override]
    public function findAllVisible(Locale $locale): array
    {
        return array_values(array_filter(
            $this->recipes,
            static fn (Recipe $recipe): bool => $recipe->isVisible() && $recipe->getLocale() === $locale,
        ));
    }

    #[\Override]
    public function findOneBySlug(string $slug): ?Recipe
    {
        foreach ($this->recipes as $recipe) {
            if ($recipe->getSlug() === $slug) {
                return $recipe;
            }
        }

        return null;
    }

    #[\Override]
    public function findLatestUpdated(): ?Recipe
    {
        return $this->recipes[0] ?? null;
    }
}
```

- [ ] **Step 2: Écrire les tests des contrôleurs**

`tests/Recipes/Infrastructure/Symfony/Controller/ListControllerTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\Symfony\Controller;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Recipes\Infrastructure\Symfony\Controller\ListController;
use App\Shared\Domain\Data\Locale;
use App\Tests\Recipes\Infrastructure\Double\Repository\FakeRecipeRepository;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;
use Symfony\Component\HttpFoundation\Request;

#[CoversClass(ListController::class)]
#[UsesClass(Recipe::class)]
final class ListControllerTest extends WebTestCase
{
    #[\Override]
    protected function tearDown(): void
    {
        self::ensureKernelShutdown();
    }

    public function testItOnlyListsVisibleRecipesOfTheCurrentLocale(): void
    {
        $repository = new FakeRecipeRepository();
        $repository->add((new Recipe())->setName('Visible English')->setSlug('visible-en')->setVisible(true));
        $repository->add((new Recipe())->setName('Hidden English')->setSlug('hidden-en'));
        $repository->add((new Recipe())->setName('Visible French')->setSlug('visible-fr')->setVisible(true)->setLocale(Locale::FR));

        $client = self::createClient();
        $client->getContainer()->set(RecipeRepository::class, $repository);

        $crawler = $client->request(Request::METHOD_GET, '/recipes');

        self::assertResponseIsSuccessful();
        $this->assertStringContainsString('Visible English', $crawler->text());
        $this->assertStringNotContainsString('Hidden English', $crawler->text());
        $this->assertStringNotContainsString('Visible French', $crawler->text());
    }

    public function testTheFrenchListLivesUnderItsOwnPath(): void
    {
        $repository = new FakeRecipeRepository();
        $repository->add((new Recipe())->setName('Visible French')->setSlug('visible-fr')->setVisible(true)->setLocale(Locale::FR));

        $client = self::createClient();
        $client->getContainer()->set(RecipeRepository::class, $repository);

        $crawler = $client->request(Request::METHOD_GET, '/fr/recettes');

        self::assertResponseIsSuccessful();
        $this->assertStringContainsString('Visible French', $crawler->text());
    }
}
```

`tests/Recipes/Infrastructure/Symfony/Controller/ViewControllerTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\Symfony\Controller;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Recipes\Infrastructure\Symfony\Controller\ViewController;
use App\Shared\Domain\Data\Locale;
use App\Tests\Recipes\Infrastructure\Double\Repository\FakeRecipeRepository;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;

#[CoversClass(ViewController::class)]
#[UsesClass(Recipe::class)]
#[UsesClass(RecipeContent::class)]
#[UsesClass(Step::class)]
#[UsesClass(Ingredient::class)]
final class ViewControllerTest extends WebTestCase
{
    #[\Override]
    protected function tearDown(): void
    {
        self::ensureKernelShutdown();
    }

    private static function visibleRecipe(Locale $locale = Locale::EN): Recipe
    {
        return (new Recipe())
            ->setName('Udon noodles')
            ->setSlug('udon')
            ->setVisible(true)
            ->setLocale($locale)
            ->setContent(new RecipeContent([
                new Step('s1', 'Cuire les nouilles', [new Ingredient('i1', 'de nouilles Udon', 300.0)]),
            ]));
    }

    public function testItRendersAVisibleRecipe(): void
    {
        $repository = new FakeRecipeRepository();
        $repository->add(self::visibleRecipe());

        $client = self::createClient();
        $client->getContainer()->set(RecipeRepository::class, $repository);

        $client->request(Request::METHOD_GET, '/recipes/udon');

        self::assertResponseIsSuccessful();
    }

    public function testAHiddenRecipeIsNotFound(): void
    {
        $repository = new FakeRecipeRepository();
        $repository->add((new Recipe())->setName('Draft')->setSlug('draft'));

        $client = self::createClient();
        $client->getContainer()->set(RecipeRepository::class, $repository);

        $client->request(Request::METHOD_GET, '/recipes/draft');

        self::assertResponseStatusCodeSame(Response::HTTP_NOT_FOUND);
    }

    public function testAnUnknownSlugIsNotFound(): void
    {
        $client = self::createClient();
        $client->getContainer()->set(RecipeRepository::class, new FakeRecipeRepository());

        $client->request(Request::METHOD_GET, '/recipes/nope');

        self::assertResponseStatusCodeSame(Response::HTTP_NOT_FOUND);
    }

    public function testAFrenchRecipeRedirectsToItsOwnLocale(): void
    {
        $repository = new FakeRecipeRepository();
        $repository->add(self::visibleRecipe(Locale::FR));

        $client = self::createClient();
        $client->getContainer()->set(RecipeRepository::class, $repository);

        $client->request(Request::METHOD_GET, '/recipes/udon');

        self::assertResponseStatusCodeSame(Response::HTTP_MOVED_PERMANENTLY);
        self::assertResponseRedirects('/fr/recettes/udon');
    }
}
```

- [ ] **Step 3: Lancer les tests et vérifier qu'ils échouent**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/Symfony`
Expected: FAIL, 404 sur `/recipes` (route inexistante).

- [ ] **Step 4: Écrire le contrôleur de liste**

`src/Recipes/Infrastructure/Symfony/Controller/ListController.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Symfony\Controller;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Data\RecipeType;
use App\Recipes\Domain\Data\Route as RouteRecipes;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Shared\Domain\Data\Locale;
use App\Shared\Domain\HttpCache\CacheMethodsTrait;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;

final class ListController extends AbstractController
{
    use CacheMethodsTrait;

    #[Route(path: [
        'en' => '/recipes',
        'fr' => '/fr/recettes',
    ], name: RouteRecipes::LIST->value, options: [
        'sitemap' => true,
    ], methods: ['GET'])]
    public function __invoke(Request $request, RecipeRepository $recipeRepository): Response
    {
        $latest = $recipeRepository->findLatestUpdated();

        $response = new Response();
        $response->setEtag(self::computeEtag('recipes', $latest));
        $response->setLastModified($latest?->getUpdatedAt());
        $response->setMaxAge(60 * 60 * 24 * 30);
        $response->setPublic();

        if ($response->isNotModified($request)) {
            return $response;
        }

        $recipes = $recipeRepository->findAllVisible(Locale::from($request->getLocale()));

        $grouped = [];
        foreach (RecipeType::cases() as $type) {
            $ofType = array_values(array_filter($recipes, static fn (Recipe $r): bool => $r->getType() === $type));
            if ($ofType !== []) {
                $grouped[$type->value] = [
                    'type' => $type,
                    'recipes' => $ofType,
                ];
            }
        }

        return $this->render('recipes/list.html.twig', [
            'grouped_recipes' => $grouped,
        ], $response);
    }
}
```

**Signature vérifiée:** `CacheMethodsTrait::computeEtag(string|CacheableEntity|null ...$content): string` est variadique et accepte les `null`, donc `self::computeEtag('recipes', $latest)` fonctionne tel quel même sans recette en base.

- [ ] **Step 5: Écrire le contrôleur de vue**

`src/Recipes/Infrastructure/Symfony/Controller/ViewController.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Symfony\Controller;

use App\Recipes\Domain\Data\Route as RouteRecipes;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Shared\Domain\HttpCache\CacheMethodsTrait;
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Request;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Attribute\Route;

final class ViewController extends AbstractController
{
    use CacheMethodsTrait;

    #[Route(path: [
        'en' => '/recipes/{slug}',
        'fr' => '/fr/recettes/{slug}',
    ], name: RouteRecipes::VIEW->value, methods: ['GET'])]
    public function __invoke(string $slug, Request $request, RecipeRepository $recipeRepository): Response
    {
        $recipe = $recipeRepository->findOneBySlug($slug);

        if ($recipe === null || ! $recipe->isVisible()) {
            throw $this->createNotFoundException();
        }

        if ($request->getLocale() !== $recipe->getLocale()->value) {
            return $this->redirectToRoute(RouteRecipes::VIEW->value, [
                'slug' => $recipe->getSlug(),
                '_locale' => $recipe->getLocale()
                    ->value,
            ], Response::HTTP_MOVED_PERMANENTLY);
        }

        $response = new Response();
        $response->setEtag(self::computeEtag($recipe));
        $response->setLastModified($recipe->getUpdatedAt());
        $response->setMaxAge(60 * 60 * 24 * 30);
        $response->setPublic();

        if ($response->isNotModified($request)) {
            return $response;
        }

        return $this->render('recipes/view.html.twig', [
            'recipe' => $recipe,
        ], $response);
    }
}
```

- [ ] **Step 6: Écrire les templates de page**

La convention de page a été vérifiée dans le code: `base.html.twig` expose `{% block main %}` **à l'intérieur** de `{% block body %}`, lequel porte déjà l'en-tête et le pied de page. Toutes les pages du site (`cv/home`, `blog/home`, `places/home`) surchargent `main`, jamais `body`. Le titre suit le motif `X | Hugo Alliaume`. Les liens vers une route d'un autre contexte passent par `constant()`, comme dans `blog/home.html.twig`.

`templates/recipes/list.html.twig`:

```twig
{% extends 'base.html.twig' %}

{% block title %}{{ 'recipes.title'|trans }} | Hugo Alliaume{% endblock %}

{% block main %}
<main class="container mx-auto p-2 sm:py-4 max-w-screen-lg">
    <h1 class="text-3xl sm:text-5xl font-extrabold mb-6 sm:mb-8 tracking-tight text-center">
        {{ 'recipes.title'|trans }}
    </h1>

    {% for group in grouped_recipes %}
        <h2 class="mt-8 mb-3 text-xl sm:text-2xl font-bold tracking-tight">{{ group.type.toTranslatable|trans }}</h2>
        <ul class="flex flex-col gap-2">
            {% for recipe in group.recipes %}
                <li>
                    <a href="{{ path(constant('App\\Recipes\\Domain\\Data\\Route::VIEW').value, { slug: recipe.slug }) }}"
                       class="hover:text-primary-700 dark:hover:text-slate-400 transition-colors">{{ recipe.name }}</a>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>{{ 'recipes.empty'|trans }}</p>
    {% endfor %}
</main>
{% endblock %}
```

`templates/recipes/view.html.twig`:

```twig
{% extends 'base.html.twig' %}

{% block title %}{{ recipe.name }} | Hugo Alliaume{% endblock %}

{% block main %}
<main class="container mx-auto p-2 sm:py-4 max-w-screen-lg">
    <article data-controller="recipe-servings recipe-progress"
             data-recipe-servings-base-value="{{ recipe.servings }}"
             data-recipe-progress-key-value="{{ recipe.slug }}">
        <h1 class="text-3xl sm:text-5xl font-extrabold mb-2 tracking-tight">{{ recipe.name }}</h1>
        <p class="mb-6 text-sm text-zinc-500">{{ recipe.type.toTranslatable|trans }}</p>

        <div class="mb-6 flex items-center gap-2">
            <label for="recipe-servings" class="text-sm">{{ 'recipes.servings_label'|trans }}</label>
            <button type="button" data-action="recipe-servings#decrease" aria-label="{{ 'recipes.decrease'|trans }}" class="px-2">&minus;</button>
            <input id="recipe-servings" type="number" min="1" step="1"
                   value="{{ recipe.servings }}"
                   data-recipe-servings-target="input"
                   data-action="input->recipe-servings#update"
                   class="w-16 rounded border px-2 py-1 text-center">
            <button type="button" data-action="recipe-servings#increase" aria-label="{{ 'recipes.increase'|trans }}" class="px-2">+</button>
            <button type="button" data-action="recipe-progress#reset" class="ml-4 text-sm underline">{{ 'recipes.reset'|trans }}</button>
        </div>

        <twig:Recipe:Grid :recipe="recipe" />
    </article>
</main>
{% endblock %}
```

- [ ] **Step 7: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes`
Expected: PASS, l'ensemble de la suite du contexte.

- [ ] **Step 8: Vérifier les routes générées**

Run: `symfony console debug:router | grep recipes`
Expected: quatre lignes, `app.recipes.list.en`, `app.recipes.list.fr`, `app.recipes.view.en`, `app.recipes.view.fr`.

- [ ] **Step 9: Style, lint, analyse**

Run: `make cs.fix && make lint.back && make phpstan && make archi`

- [ ] **Step 10: Commit**

```bash
git add src/Recipes templates/recipes tests/Recipes
git commit -m "feat(recipes): ajoute les pages publiques liste et détail"
```

---

### Task 10: Sitemap et navigation

**Files:**

- Create: `src/Recipes/Infrastructure/Symfony/EventListener/SitemapListener.php`
- Modify: `config/services.yaml` (paramètre `app.menu_definition`)
- Test: `tests/Recipes/Infrastructure/Symfony/EventListener/SitemapListenerTest.php`

**Interfaces:**

- Consumes: `RecipeRepository`, `Route::VIEW`, `Locale`.
- Produces: une `UrlConcrete` par recette visible dans la section `recipes` du sitemap.

`app.recipes.list` porte déjà `sitemap: true`, ce qui suffit pour une route sans paramètre. `app.recipes.view` a un `{slug}` et doit donc être peuplée à la main, sous la locale de chaque recette et non sous chacune des locales activées. Lire `src/Blog/Infrastructure/Symfony/EventListener/SitemapListener.php` avant d'écrire celui-ci.

- [ ] **Step 1: Écrire le test**

`tests/Recipes/Infrastructure/Symfony/EventListener/SitemapListenerTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\Symfony\EventListener;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Infrastructure\Symfony\EventListener\SitemapListener;
use App\Shared\Domain\Data\Locale;
use App\Tests\Recipes\Infrastructure\Double\Repository\FakeRecipeRepository;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use Presta\SitemapBundle\Event\SitemapPopulateEvent;
use Presta\SitemapBundle\Service\UrlContainerInterface;
use Presta\SitemapBundle\Sitemap\Url\UrlConcrete;
use PHPUnit\Framework\TestCase;
use Symfony\Component\Routing\Generator\UrlGeneratorInterface;

#[CoversClass(SitemapListener::class)]
#[UsesClass(Recipe::class)]
final class SitemapListenerTest extends TestCase
{
    public function testItAddsOneUrlPerVisibleRecipeUnderItsOwnLocale(): void
    {
        $repository = new FakeRecipeRepository();
        $repository->add((new Recipe())->setSlug('udon')->setVisible(true));
        $repository->add((new Recipe())->setSlug('tarte')->setVisible(true)->setLocale(Locale::FR));
        $repository->add((new Recipe())->setSlug('draft'));

        $added = [];
        $container = $this->createMock(UrlContainerInterface::class);
        $container->method('addUrl')
            ->willReturnCallback(static function (UrlConcrete $url, string $section) use (&$added): void {
                $added[$section][] = $url->getLoc();
            });

        $urlGenerator = $this->createMock(UrlGeneratorInterface::class);
        $urlGenerator->method('generate')
            ->willReturnCallback(static fn (string $route, array $params): string => \sprintf(
                'https://example.test/%s/%s',
                $params['_locale'],
                $params['slug'],
            ));

        $event = new SitemapPopulateEvent($container, $urlGenerator);

        (new SitemapListener($repository))($event);

        $this->assertSame([
            'recipes' => [
                'https://example.test/en/udon',
                'https://example.test/fr/tarte',
            ],
        ], $added);
    }
}
```

**Note:** la signature de `SitemapPopulateEvent::__construct()` varie selon les versions de `presta/sitemap-bundle`. Vérifier avec `cat vendor/presta/sitemap-bundle/src/Event/SitemapPopulateEvent.php` et adapter la construction de l'événement, sans toucher aux assertions.

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/Symfony/EventListener`
Expected: FAIL, classe `SitemapListener` introuvable.

- [ ] **Step 3: Écrire le listener**

`src/Recipes/Infrastructure/Symfony/EventListener/SitemapListener.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\Symfony\EventListener;

use App\Recipes\Domain\Data\Route as RouteRecipes;
use App\Recipes\Domain\Repository\RecipeRepository;
use App\Shared\Domain\Data\Locale;
use Presta\SitemapBundle\Event\SitemapPopulateEvent;
use Presta\SitemapBundle\Sitemap\Url\UrlConcrete;
use Symfony\Component\EventDispatcher\Attribute\AsEventListener;
use Symfony\Component\Routing\Generator\UrlGeneratorInterface;

#[AsEventListener(priority: 100)]
final readonly class SitemapListener
{
    public function __construct(
        private RecipeRepository $recipeRepository,
    ) {
    }

    public function __invoke(SitemapPopulateEvent $event): void
    {
        $urls = $event->getUrlContainer();
        $urlGenerator = $event->getUrlGenerator();

        foreach (Locale::cases() as $locale) {
            foreach ($this->recipeRepository->findAllVisible($locale) as $recipe) {
                $urls->addUrl(
                    new UrlConcrete(
                        $urlGenerator->generate(RouteRecipes::VIEW->value, [
                            'slug' => $recipe->getSlug(),
                            '_locale' => $locale->value,
                        ], UrlGeneratorInterface::ABSOLUTE_URL),
                        $recipe->getUpdatedAt(),
                    ),
                    'recipes',
                );
            }
        }
    }
}
```

- [ ] **Step 4: Ajouter l'entrée de navigation**

Dans `config/services.yaml`, à la fin de `app.menu_definition`:

```yaml
- label: "nav.recipes"
  route: !php/enum App\Recipes\Domain\Data\Route::LIST->value
  extras:
      routes:
          - route: !php/enum App\Recipes\Domain\Data\Route::VIEW->value
```

- [ ] **Step 5: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes`
Expected: PASS.

- [ ] **Step 6: Vérifier le sitemap et le menu de bout en bout**

Run: `symfony console presta:sitemaps:dump --no-interaction && grep -c recettes public/sitemap.recipes.xml`
Expected: le fichier `public/sitemap.recipes.xml` existe. S'il n'y a aucune recette visible en base, le fichier peut être absent: créer alors une recette visible via `symfony console doctrine:query:sql` ou l'admin, ou se contenter du test unitaire.

Run: `symfony console debug:container --parameter=app.menu_definition`
Expected: l'entrée `nav.recipes` est présente.

- [ ] **Step 7: Style, lint, analyse**

Run: `make cs.fix && make lint.back && make phpstan && make archi`

- [ ] **Step 8: Commit**

```bash
git add src/Recipes config/services.yaml tests/Recipes
git commit -m "feat(recipes): ajoute les recettes au sitemap et à la navigation"
```

---

### Task 11: Contrôleur Stimulus du multiplicateur de portions

**Files:**

- Create: `assets/controllers/recipe_servings_controller.js`

**Interfaces:**

- Consumes: le balisage des tâches 7 à 9 — `data-recipe-servings-base-value` sur l'article, la cible `input`, les cibles `quantity` et `unit`, et sur chaque élément d'ingrédient `data-qty-min`, `data-qty-max`, `data-unit-one`, `data-unit-other`.
- Produits: aucun autre code ne dépend de ce contrôleur.

Le projet n'a pas de runner de tests JavaScript. La vérification est manuelle, décrite au Step 3.

Le formateur reproduit **à l'identique** les règles de `QuantityFormatter` (tâche 5): entier tel quel, fraction vulgaire à 0,01 près, sinon deux décimales.

- [ ] **Step 1: Écrire le contrôleur**

`assets/controllers/recipe_servings_controller.js`:

```js
import { Controller } from "@hotwired/stimulus";

const FRACTIONS = [
    [0.125, "⅛"],
    [0.25, "¼"],
    [1 / 3, "⅓"],
    [0.375, "⅜"],
    [0.5, "½"],
    [0.625, "⅝"],
    [2 / 3, "⅔"],
    [0.75, "¾"],
    [0.875, "⅞"],
];
const TOLERANCE = 0.01;

/* stimulusFetch: 'lazy' */
export default class extends Controller {
    static targets = ["input"];
    static values = { base: Number };

    connect() {
        this.locale = document.documentElement.lang || "en";
        this.pluralRules = new Intl.PluralRules(this.locale);
        this.numberFormat = new Intl.NumberFormat(this.locale, { maximumFractionDigits: 2 });
        this.rangeSeparator = this.locale.startsWith("fr") ? " à " : " to ";
        this.update();
    }

    decrease() {
        this.inputTarget.value = Math.max(1, Number(this.inputTarget.value) - 1);
        this.update();
    }

    increase() {
        this.inputTarget.value = Number(this.inputTarget.value) + 1;
        this.update();
    }

    update() {
        const servings = Number(this.inputTarget.value);
        if (!Number.isFinite(servings) || servings < 1 || !this.baseValue) return;

        const factor = servings / this.baseValue;

        for (const element of this.element.querySelectorAll("[data-qty-min]")) {
            const quantity = element.querySelector("[data-recipe-servings-target='quantity']");
            if (!quantity) continue;

            const min = Number(element.dataset.qtyMin) * factor;
            const max = element.dataset.qtyMax ? Number(element.dataset.qtyMax) * factor : null;

            let text =
                max === null
                    ? this._format(min)
                    : this._format(min) + this.rangeSeparator + this._format(max);

            const one = element.dataset.unitOne;
            const other = element.dataset.unitOther;
            if (one && other) {
                text += " " + (this.pluralRules.select(max ?? min) === "one" ? one : other);
            }

            quantity.textContent = text;
        }
    }

    _format(value) {
        const whole = Math.floor(value);
        const fraction = value - whole;

        if (fraction < TOLERANCE || fraction > 1 - TOLERANCE) {
            return String(Math.round(value));
        }

        for (const [decimal, glyph] of FRACTIONS) {
            if (Math.abs(fraction - decimal) < TOLERANCE) {
                return whole > 0 ? `${whole} ${glyph}` : glyph;
            }
        }

        return this.numberFormat.format(value);
    }
}
```

- [ ] **Step 2: Vérifier le lint et le format**

Run: `pnpm lint && pnpm fmt:check`
Expected: aucune erreur. Si `fmt:check` échoue, lancer `pnpm fmt`.

- [ ] **Step 3: Vérifier le comportement dans le navigateur**

Run: `make start` puis ouvrir `/recipes/<slug>` d'une recette visible.

À vérifier, avec une recette ayant `servings = 4` et un ingrédient `min = 2, max = 3, unit = tbsp`:

- à 4 personnes, la cellule affiche `2 à 3 cuillères à soupe`;
- à 8 personnes, `4 à 6 cuillères à soupe`;
- à 2 personnes, `1 à 1 ½ cuillères à soupe`;
- un ingrédient sans `data-qty-min` (`sel, poivre`) n'est jamais modifié;
- un ingrédient à `min = 0.5` sans unité passe de `½` à `1 ½` quand on triple.

- [ ] **Step 4: Commit**

```bash
git add assets/controllers/recipe_servings_controller.js
git commit -m "feat(recipes): ajoute le multiplicateur de portions côté client"
```

---

### Task 12: Contrôleur Stimulus du suivi d'avancement

**Files:**

- Create: `assets/controllers/recipe_progress_controller.js`

**Interfaces:**

- Consumes: `data-recipe-progress-key-value` sur l'article, `data-node-id` sur chaque cellule et chaque élément de liste, `data-action="click->recipe-progress#toggle"`.
- Produits: la classe CSS `is-done` appliquée à tous les éléments partageant le même `data-node-id`, donc simultanément dans le rendu desktop et le rendu mobile.

- [ ] **Step 1: Écrire le contrôleur**

`assets/controllers/recipe_progress_controller.js`:

```js
import { Controller } from "@hotwired/stimulus";

/* stimulusFetch: 'lazy' */
export default class extends Controller {
    static values = { key: String };

    connect() {
        this.done = new Set(this._read());
        this._render();
    }

    toggle(event) {
        const nodeId = event.currentTarget.dataset.nodeId;
        if (!nodeId) return;

        if (this.done.has(nodeId)) {
            this.done.delete(nodeId);
        } else {
            this.done.add(nodeId);
        }

        this._write();
        this._render();
    }

    // Un <td role="button"> n'a pas l'activation clavier native d'un <button>.
    toggleOnKey(event) {
        if (event.key !== "Enter" && event.key !== " ") return;

        event.preventDefault();
        this.toggle(event);
    }

    reset() {
        this.done.clear();
        this._write();
        this._render();
    }

    _render() {
        for (const element of this.element.querySelectorAll("[data-node-id]")) {
            const isDone = this.done.has(element.dataset.nodeId);

            element.classList.toggle("is-done", isDone);

            // L'opacité et le barré ne disent rien à un lecteur d'écran.
            if (element.hasAttribute("aria-pressed")) {
                element.setAttribute("aria-pressed", isDone ? "true" : "false");
            }
        }
    }

    _storageKey() {
        return `recipe:${this.keyValue}`;
    }

    _read() {
        try {
            const raw = window.localStorage.getItem(this._storageKey());
            const parsed = raw ? JSON.parse(raw) : [];
            return Array.isArray(parsed) ? parsed : [];
        } catch {
            return [];
        }
    }

    _write() {
        try {
            window.localStorage.setItem(this._storageKey(), JSON.stringify([...this.done]));
        } catch {
            // Mode navigation privée ou quota atteint: la progression n'est simplement pas mémorisée.
        }
    }
}
```

- [ ] **Step 2: Ajouter le style de l'état terminé**

Dans `assets/styles/app.css`, à la suite des règles existantes:

```css
.is-done {
    opacity: 0.45;
    text-decoration: line-through;
}
```

- [ ] **Step 3: Vérifier le lint et le format**

Run: `pnpm lint && pnpm fmt:check`

- [ ] **Step 4: Vérifier le comportement dans le navigateur**

Ouvrir `/recipes/<slug>`. À vérifier:

- un clic sur une cellule d'étape la barre et la grise;
- recharger la page conserve l'état;
- « Réinitialiser » remet tout à zéro;
- réduire la fenêtre sous le point de rupture `md` montre l'état déjà appliqué aux mêmes nœuds dans la liste.

- [ ] **Step 5: Commit**

```bash
git add assets/controllers/recipe_progress_controller.js assets/styles/app.css
git commit -m "feat(recipes): ajoute le suivi d'avancement persisté dans le navigateur"
```

---

### Task 13: Mise en place de Vue dans la chaîne de build

**Files:**

- Modify: `package.json`
- Modify: `vite.config.ts`
- Modify: `.oxlintrc.json`
- Create: `assets/vue/Smoke.vue` (temporaire, supprimé au Step 5)

**Interfaces:**

- Produces: la capacité de compiler des composants `.vue`, consommée par la tâche 14.

Vue est la première dépendance de ce type dans le projet: cette tâche l'isole pour qu'un échec de build ne se mélange pas à la logique de l'éditeur.

- [ ] **Step 1: Installer les dépendances**

Run: `pnpm add vue && pnpm add -D @vitejs/plugin-vue`
Expected: `vue` en `dependencies`, `@vitejs/plugin-vue` en `devDependencies`, avec des versions exactes reportées dans `package.json`.

- [ ] **Step 2: Brancher le plugin Vite**

Dans `vite.config.ts`:

```ts
import { defineConfig } from "vite";
import Symfony from "@symfony/reprise/vite";
import tailwindcss from "@tailwindcss/vite";
import vue from "@vitejs/plugin-vue";

export default defineConfig({
    input: {
        app: "./assets/app.js",
        admin: "./assets/admin.js",
    },
    plugins: [
        tailwindcss(),
        vue(),
        Symfony({
            stimulus: "./assets/controllers.json",
            copy: [{ from: "./assets/images/", to: "images/" }],
        }),
    ],
    resolve: {
        alias: {
            "leaflet/dist/leaflet.min.css": "leaflet/dist/leaflet.css",
        },
    },
});
```

- [ ] **Step 3: Vérifier qu'un composant `.vue` compile**

`assets/vue/Smoke.vue`:

```vue
<script setup>
const message = "vue is wired";
</script>

<template>
    <p>{{ message }}</p>
</template>
```

Ajouter temporairement en fin de `assets/admin.js`:

```js
import "./vue/Smoke.vue";
```

Run: `pnpm build`
Expected: build réussi, aucune erreur de résolution sur `.vue`.

- [ ] **Step 4: Traiter les fichiers `.vue` dans le lint et le format**

Run: `pnpm lint && pnpm fmt:check`

Si `oxlint` ou `oxfmt` ne gèrent pas la syntaxe SFC, ajouter `"assets/vue/**"` à `ignorePatterns` dans `.oxlintrc.json` et à l'ignore de `.oxfmtrc.json`, plutôt que de laisser la commande échouer.

- [ ] **Step 5: Retirer le composant de fumée**

Run: `rm assets/vue/Smoke.vue`
Puis retirer la ligne `import "./vue/Smoke.vue";` de `assets/admin.js`.

Run: `pnpm build`
Expected: build toujours réussi.

- [ ] **Step 6: Commit**

```bash
git add package.json pnpm-lock.yaml vite.config.ts .oxlintrc.json .oxfmtrc.json
git commit -m "build: branche Vue dans la chaîne Vite"
```

---

### Task 14: Éditeur d'arbre en Vue

**Files:**

- Create: `assets/vue/RecipeEditor.vue`
- Create: `assets/vue/TreeNode.vue`
- Create: `assets/controllers/recipe_editor_controller.js`

**Interfaces:**

- Consumes: la chaîne de build de la tâche 13, et le format JSON de §1.3 de la spec.
- Produces: un contrôleur Stimulus `recipe-editor` qui lit `data-recipe-editor-content-value` (le JSON initial), monte l'application Vue et écrit le JSON sérialisé dans l'input désigné par la cible `field`. La tâche 15 fournit le balisage qui l'active.

L'arbre est manipulé en mémoire sous la forme exacte du JSON stocké. Chaque nœud créé reçoit un identifiant court et stable, sans lequel le suivi d'avancement de la tâche 12 casserait au premier réordonnancement.

- [ ] **Step 1: Écrire le composant récursif de nœud**

`assets/vue/TreeNode.vue`:

```vue
<script setup>
defineProps({
    node: { type: Object, required: true },
    depth: { type: Number, default: 0 },
});

const emit = defineEmits(["remove", "addIngredient", "addStep"]);
</script>

<template>
    <div class="tree-node" :style="{ marginLeft: `${depth * 16}px` }">
        <div v-if="node.type === 'ingredient'" class="tree-node__ingredient">
            <input
                v-model.number="node.min"
                type="number"
                step="any"
                placeholder="min"
                style="width: 5rem"
            />
            <input
                v-model.number="node.max"
                type="number"
                step="any"
                placeholder="max"
                style="width: 5rem"
            />
            <select v-model="node.unit">
                <option :value="null">—</option>
                <option v-for="unit in units" :key="unit" :value="unit">{{ unit }}</option>
            </select>
            <input
                v-model="node.label"
                type="text"
                placeholder="de farine de riz gluant"
                style="flex: 1"
            />
            <button type="button" @click="emit('remove')">✕</button>
        </div>

        <div v-else class="tree-node__step">
            <input
                v-model="node.text"
                type="text"
                placeholder="Mélanger farine et eau"
                style="flex: 1"
            />
            <button type="button" @click="emit('addIngredient', node)">+ ingrédient</button>
            <button type="button" @click="emit('addStep', node)">+ étape</button>
            <button type="button" @click="emit('remove')">✕</button>

            <TreeNode
                v-for="(child, index) in node.children"
                :key="child.id"
                :node="child"
                :depth="depth + 1"
                @remove="node.children.splice(index, 1)"
                @add-ingredient="emit('addIngredient', $event)"
                @add-step="emit('addStep', $event)"
            />
        </div>
    </div>
</template>

<script>
export const units = [
    "g",
    "kg",
    "ml",
    "cl",
    "l",
    "tbsp",
    "tsp",
    "pinch",
    "clove",
    "slice",
    "bunch",
    "sprig",
    "handful",
    "drop",
    "can",
    "pack",
];

export default {
    name: "TreeNode",
    data() {
        return { units };
    },
};
</script>
```

**Note pour l'implémenteur:** mélanger `<script setup>` et un `<script>` classique dans un même SFC est autorisé mais l'export nommé `units` doit être accessible au template. Si la compilation proteste, déplacer la liste des unités dans un `assets/vue/units.js` importé par `<script setup>`, et supprimer le second bloc `<script>` ainsi que l'option `name` (la récursion fonctionne alors via le nom de fichier).

- [ ] **Step 2: Écrire le composant racine**

`assets/vue/RecipeEditor.vue`:

```vue
<script setup>
import { computed, ref, watch } from "vue";
import TreeNode from "./TreeNode.vue";

const props = defineProps({
    initial: { type: Object, required: true },
});
const emit = defineEmits(["change"]);

const roots = ref(structuredClone(props.initial.roots ?? []));

let counter = 0;
function nextId(prefix) {
    counter += 1;
    return `${prefix}${Date.now().toString(36)}${counter}`;
}

function newIngredient() {
    return { type: "ingredient", id: nextId("i"), label: "", min: null, max: null, unit: null };
}

function newStep() {
    return { type: "step", id: nextId("s"), text: "", children: [] };
}

function addIngredient(parent) {
    (parent ? parent.children : roots.value).push(newIngredient());
}

function addStep(parent) {
    (parent ? parent.children : roots.value).push(newStep());
}

const serialized = computed(() => JSON.stringify({ roots: roots.value }));

watch(serialized, (value) => emit("change", value), { immediate: true });
</script>

<template>
    <div class="recipe-editor">
        <div class="recipe-editor__toolbar">
            <button type="button" @click="addStep(null)">+ étape racine</button>
            <button type="button" @click="addIngredient(null)">+ ingrédient racine</button>
        </div>

        <TreeNode
            v-for="(node, index) in roots"
            :key="node.id"
            :node="node"
            @remove="roots.splice(index, 1)"
            @add-ingredient="addIngredient($event)"
            @add-step="addStep($event)"
        />
    </div>
</template>
```

- [ ] **Step 3: Écrire le pont Stimulus**

`assets/controllers/recipe_editor_controller.js`:

```js
import { Controller } from "@hotwired/stimulus";
import { createApp, h } from "vue";
import RecipeEditor from "../vue/RecipeEditor.vue";

/* stimulusFetch: 'lazy' */
export default class extends Controller {
    static targets = ["field", "mount"];
    static values = { content: Object };

    connect() {
        this.app = createApp({
            render: () =>
                h(RecipeEditor, {
                    initial: this.contentValue,
                    onChange: (json) => {
                        this.fieldTarget.value = json;
                    },
                }),
        });
        this.app.mount(this.mountTarget);
    }

    disconnect() {
        this.app?.unmount();
        this.app = null;
    }
}
```

- [ ] **Step 4: Vérifier le build, le lint et le format**

Run: `pnpm build && pnpm lint && pnpm fmt:check`
Expected: build réussi. Si le lint refuse la syntaxe SFC, appliquer la décision prise au Step 4 de la tâche 13.

- [ ] **Step 5: Commit**

```bash
git add assets/vue assets/controllers/recipe_editor_controller.js
git commit -m "feat(recipes): ajoute l'éditeur d'arbre en Vue monté par Stimulus"
```

---

### Task 15: CRUD EasyAdmin et champ d'édition du contenu

**Files:**

- Create: `src/Recipes/Infrastructure/EasyAdmin/Controller/RecipeCrudController.php`
- Create: `src/Recipes/Infrastructure/EasyAdmin/Field/RecipeContentField.php`
- Create: `src/Recipes/Infrastructure/EasyAdmin/Form/RecipeContentType.php`
- Create: `src/Recipes/Infrastructure/EasyAdmin/Form/RecipeContentTransformer.php`
- Create: `templates/admin/fields/recipe_content.html.twig`
- Modify: `src/Shared/Infrastructure/EasyAdmin/Controller/DashboardController.php`
- Test: `tests/Recipes/Infrastructure/EasyAdmin/Form/RecipeContentTransformerTest.php`

**Interfaces:**

- Consumes: `Recipe` (tâche 3), `RecipeContentType` DBAL (tâche 2) pour la logique de mapping, le contrôleur `recipe-editor` (tâche 14).
- Produces: l'écran d'administration complet.

Attention à la collision de noms: `App\Recipes\Infrastructure\Doctrine\DBAL\Type\RecipeContentType` (Doctrine) et `App\Recipes\Infrastructure\EasyAdmin\Form\RecipeContentType` (formulaire) sont deux classes distinctes. Les importer sous alias là où les deux apparaissent.

Le transformateur convertit un `MappingError` en `TransformationFailedException`, donc en erreur de formulaire. Le laisser remonter depuis le type DBAL produirait une erreur 500 au flush, bien après que l'utilisateur ait quitté la page.

- [ ] **Step 1: Écrire le test du transformateur**

`tests/Recipes/Infrastructure/EasyAdmin/Form/RecipeContentTransformerTest.php`:

```php
<?php

declare(strict_types=1);

namespace App\Tests\Recipes\Infrastructure\EasyAdmin\Form;

use App\Recipes\Domain\Data\Ingredient;
use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Domain\Data\Step;
use App\Recipes\Infrastructure\Doctrine\DBAL\Type\RecipeContentType as RecipeContentDbalType;
use App\Recipes\Infrastructure\EasyAdmin\Form\RecipeContentTransformer;
use PHPUnit\Framework\Attributes\CoversClass;
use PHPUnit\Framework\Attributes\UsesClass;
use PHPUnit\Framework\TestCase;
use Symfony\Component\Form\Exception\TransformationFailedException;

#[CoversClass(RecipeContentTransformer::class)]
#[UsesClass(RecipeContentDbalType::class)]
#[UsesClass(RecipeContent::class)]
#[UsesClass(Step::class)]
#[UsesClass(Ingredient::class)]
final class RecipeContentTransformerTest extends TestCase
{
    public function testItRoundTrips(): void
    {
        $transformer = new RecipeContentTransformer(new RecipeContentDbalType());
        $content = new RecipeContent([
            new Step('s1', 'Mélanger', [new Ingredient('i1', 'de farine', 200.0)]),
        ]);

        $json = $transformer->transform($content);
        $this->assertIsString($json);
        $this->assertEquals($content, $transformer->reverseTransform($json));
    }

    public function testAnEmptyValueBecomesAnEmptyContent(): void
    {
        $transformer = new RecipeContentTransformer(new RecipeContentDbalType());

        $this->assertEquals(new RecipeContent(), $transformer->reverseTransform(''));
        $this->assertEquals(new RecipeContent(), $transformer->reverseTransform(null));
    }

    public function testMalformedJsonBecomesAFormError(): void
    {
        $transformer = new RecipeContentTransformer(new RecipeContentDbalType());

        $this->expectException(TransformationFailedException::class);

        $transformer->reverseTransform('{"roots": [{"type": "step"}]}');
    }
}
```

- [ ] **Step 2: Lancer le test et vérifier qu'il échoue**

Run: `symfony php vendor/bin/phpunit tests/Recipes/Infrastructure/EasyAdmin`
Expected: FAIL, classe `RecipeContentTransformer` introuvable.

- [ ] **Step 3: Écrire le transformateur**

`src/Recipes/Infrastructure/EasyAdmin/Form/RecipeContentTransformer.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\EasyAdmin\Form;

use App\Recipes\Domain\Data\RecipeContent;
use App\Recipes\Infrastructure\Doctrine\DBAL\Type\RecipeContentType as RecipeContentDbalType;
use Doctrine\DBAL\Platforms\PostgreSQLPlatform;
use Doctrine\DBAL\Types\ConversionException;
use Symfony\Component\Form\DataTransformerInterface;
use Symfony\Component\Form\Exception\TransformationFailedException;

/**
 * @implements DataTransformerInterface<RecipeContent, string>
 */
final readonly class RecipeContentTransformer implements DataTransformerInterface
{
    private PostgreSQLPlatform $platform;

    public function __construct(
        private RecipeContentDbalType $type = new RecipeContentDbalType(),
    ) {
        $this->platform = new PostgreSQLPlatform();
    }

    #[\Override]
    public function transform(mixed $value): string
    {
        return $this->type->convertToDatabaseValue($value ?? new RecipeContent(), $this->platform) ?? '';
    }

    #[\Override]
    public function reverseTransform(mixed $value): RecipeContent
    {
        if ($value === null || $value === '') {
            return new RecipeContent();
        }

        try {
            return $this->type->convertToPHPValue($value, $this->platform) ?? new RecipeContent();
        } catch (ConversionException $conversionException) {
            throw new TransformationFailedException(
                'Le contenu de la recette est invalide.',
                previous: $conversionException,
            );
        }
    }
}
```

**Note:** un `readonly` avec une propriété assignée dans le constructeur est valide. Si PHPStan proteste sur la valeur par défaut d'un paramètre objet, remplacer par une assignation dans le corps du constructeur.

- [ ] **Step 4: Écrire le type de formulaire et le champ EasyAdmin**

`src/Recipes/Infrastructure/EasyAdmin/Form/RecipeContentType.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\EasyAdmin\Form;

use Symfony\Component\Form\AbstractType;
use Symfony\Component\Form\Extension\Core\Type\HiddenType;
use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolver;

/**
 * @extends AbstractType<mixed>
 */
final class RecipeContentType extends AbstractType
{
    public function __construct(
        private readonly RecipeContentTransformer $transformer,
    ) {
    }

    /**
     * @param array<string, mixed> $options
     */
    #[\Override]
    public function buildForm(FormBuilderInterface $builder, array $options): void
    {
        $builder->addModelTransformer($this->transformer);
    }

    #[\Override]
    public function configureOptions(OptionsResolver $resolver): void
    {
        $resolver->setDefaults([
            'compound' => false,
        ]);
    }

    #[\Override]
    public function getParent(): string
    {
        return HiddenType::class;
    }

    /**
     * C'est ce préfixe qui fait chercher à Symfony le bloc `recipe_content_widget`
     * du thème de formulaire, avant de retomber sur `hidden_widget`.
     */
    #[\Override]
    public function getBlockPrefix(): string
    {
        return 'recipe_content';
    }
}
```

`src/Recipes/Infrastructure/EasyAdmin/Field/RecipeContentField.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\EasyAdmin\Field;

use App\Recipes\Infrastructure\EasyAdmin\Form\RecipeContentType;
use EasyCorp\Bundle\EasyAdminBundle\Contracts\Field\FieldInterface;
use EasyCorp\Bundle\EasyAdminBundle\Field\FieldTrait;

final class RecipeContentField implements FieldInterface
{
    use FieldTrait;

    public static function new(string $propertyName, ?string $label = null): self
    {
        return (new self())
            ->setProperty($propertyName)
            ->setLabel($label)
            ->setFormType(RecipeContentType::class)
            ->addFormTheme('admin/fields/recipe_content.html.twig')
            ->onlyOnForms()
        ;
    }
}
```

**Attention:** `setTemplatePath()` ne convient pas ici. Dans EasyAdmin, il gouverne le rendu des pages _index_ et _detail_, pas celui du widget de formulaire. Le rendu d'un widget passe par un **thème de formulaire**, d'où `addFormTheme()` et le bloc `recipe_content_widget` du Step 5.

**Vérifié:** `FieldTrait::addFormTheme(string ...$formThemePaths): self` existe bien dans la version installée (`vendor/easycorp/easyadmin-bundle/src/Field/FieldTrait.php:311`), aux côtés de `setTemplatePath()` (ligne 303) qui, lui, ne sert qu'aux pages index et detail.

Le seul champ custom existant du projet, `templates/admin/fields/post_status.html.twig`, est un template de rendu `setTemplatePath` (page index) et non un thème de formulaire: il illustre la variable `field` disponible en lecture, pas le cas qui nous occupe.

- [ ] **Step 5: Écrire le thème de formulaire**

`templates/admin/fields/recipe_content.html.twig`:

```twig
{% block recipe_content_widget %}
    <div data-controller="recipe-editor"
         data-recipe-editor-content-value="{{ value is empty ? '{"roots":[]}' : value }}">
        <input type="hidden" {{ block('widget_attributes') }} value="{{ value }}" data-recipe-editor-target="field">
        <div data-recipe-editor-target="mount"></div>
    </div>
{% endblock %}
```

Le `<input>` est écrit à la main plutôt que via `form_widget(form)`: appeler `form_widget` depuis l'intérieur du bloc `_widget` du même champ produirait une récursion infinie. Le bloc `widget_attributes`, fourni par le thème de formulaire de Symfony, se charge des attributs `id`, `name` et `required`.

- [ ] **Step 6: Écrire le contrôleur CRUD**

`src/Recipes/Infrastructure/EasyAdmin/Controller/RecipeCrudController.php`:

```php
<?php

declare(strict_types=1);

namespace App\Recipes\Infrastructure\EasyAdmin\Controller;

use App\Recipes\Domain\Data\Recipe;
use App\Recipes\Domain\Data\RecipeType;
use App\Recipes\Infrastructure\EasyAdmin\Field\RecipeContentField;
use App\Shared\Domain\Data\Locale;
use EasyCorp\Bundle\EasyAdminBundle\Config\Crud;
use EasyCorp\Bundle\EasyAdminBundle\Controller\AbstractCrudController;
use EasyCorp\Bundle\EasyAdminBundle\Field\BooleanField;
use EasyCorp\Bundle\EasyAdminBundle\Field\ChoiceField;
use EasyCorp\Bundle\EasyAdminBundle\Field\IdField;
use EasyCorp\Bundle\EasyAdminBundle\Field\IntegerField;
use EasyCorp\Bundle\EasyAdminBundle\Field\TextField;

/**
 * @extends AbstractCrudController<Recipe>
 */
class RecipeCrudController extends AbstractCrudController
{
    #[\Override]
    public static function getEntityFqcn(): string
    {
        return Recipe::class;
    }

    #[\Override]
    public function configureCrud(Crud $crud): Crud
    {
        return $crud
            ->setDefaultSort([
                'updatedAt' => 'DESC',
            ])
            ->showEntityActionsInlined()
        ;
    }

    #[\Override]
    public function configureFields(string $pageName): iterable
    {
        yield IdField::new('id')->hideOnForm();
        yield BooleanField::new('visible');
        yield TextField::new('name');
        yield TextField::new('slug');
        yield ChoiceField::new('type')->setFormTypeOption('class', RecipeType::class);
        yield ChoiceField::new('locale')->setFormTypeOption('class', Locale::class);
        yield IntegerField::new('servings');
        yield RecipeContentField::new('content');
    }
}
```

**Note:** EasyAdmin 5 sait rendre un `ChoiceField` sur un enum PHP. Si la configuration exacte diffère, s'aligner sur ce que fait déjà `Blog\…\PostCrudController` pour son champ `status`.

- [ ] **Step 7: Ajouter l'entrée au tableau de bord**

Dans `src/Shared/Infrastructure/EasyAdmin/Controller/DashboardController.php`, après la section `Places`:

```php
        yield MenuItem::section('Recipes');
        yield MenuItem::linkTo(Recipes\Infrastructure\EasyAdmin\Controller\RecipeCrudController::class, 'Recipes', 'fas fa-utensils');
```

Ajouter `use App\Recipes;` aux imports, à côté de `use App\Places;`.

- [ ] **Step 8: Lancer les tests et vérifier qu'ils passent**

Run: `symfony php vendor/bin/phpunit tests/Recipes`
Expected: PASS, toute la suite du contexte.

- [ ] **Step 9: Vérifier l'écran d'administration de bout en bout**

Run: `pnpm build && make start`, puis se connecter à `/admin` et ouvrir « Recipes ».

À vérifier:

- créer une recette, ajouter une étape racine, y ajouter deux ingrédients, enregistrer;
- rouvrir la recette: l'arbre est rechargé tel quel;
- passer `visible` à vrai et ouvrir la page publique: la grille reflète l'arbre saisi;
- coller un JSON invalide dans le champ caché via les outils de développement puis soumettre: le formulaire affiche une erreur, pas une page 500.

- [ ] **Step 10: Style, lint, analyse, architecture**

Run: `make cs.fix && make lint.back && make phpstan && make archi`

- [ ] **Step 11: Commit**

```bash
git add src/Recipes src/Shared/Infrastructure/EasyAdmin templates/admin/fields tests/Recipes
git commit -m "feat(recipes): ajoute l'écran d'administration avec l'éditeur d'arbre"
```

---

### Task 16: Vérification finale et pull request

**Files:**

- Aucun fichier applicatif nouveau.

- [ ] **Step 1: Lancer la suite complète**

Run: `make qa`
Expected: `archi`, `refactor`, `cs`, `lint`, `phpstan` et `test` passent tous. Corriger avant de continuer, sans ajouter de ligne au baseline PHPStan.

- [ ] **Step 2: Vérifier que la migration s'applique sur une base vierge**

Run: `symfony console doctrine:database:drop --force --if-exists --env=test && symfony console doctrine:database:create --env=test && symfony console doctrine:migrations:migrate --no-interaction --all-or-nothing --env=test`
Expected: toutes les migrations passent dans l'ordre, sans erreur sur `recipe`.

- [ ] **Step 3: Vérifier le build front en mode production**

Run: `pnpm build`
Expected: build réussi, et `public/build/entrypoints.json` contient bien les entrées `app` et `admin`.

- [ ] **Step 4: Relire le diff**

Run: `git diff main...HEAD --stat`
Vérifier qu'aucun fichier hors périmètre n'a été touché, en particulier que `deptrac_domains.yaml` est absent du diff.

- [ ] **Step 5: Ouvrir la pull request**

Run: `git remote -v && gh repo view --json nameWithOwner`
Confirmer qu'il n'y a pas de fork (dépôt personnel, `origin` canonique) avant de pousser.

Run: `git push -u origin feat/recipes-bounded-context`

Rédiger ensuite la description de PR: contexte, décisions structurantes (arbre en `jsonb`, colonne unique, multiplicateur côté client, éditeur Vue), et ce qui reste hors périmètre. Faire passer le texte par l'agent `natural-writing-editor` et le recopier verbatim, sans retour à la ligne en milieu de paragraphe.

---

## Suivi hors périmètre

Un point identifié pendant la conception et volontairement laissé de côté: `deptrac_domains.yaml` ne couvre aucune classe (ses regex ciblent `App\Domain\Blog\` au lieu de `App\Blog\Domain\`), donc l'isolation entre bounded contexts n'est vérifiée par personne aujourd'hui. Le réparer suppose de corriger les cinq regex et d'ajouter une layer `Vendor` attrape-tout, sous peine de faire échouer `make archi` qui passe `--fail-on-uncovered`. À traiter dans une PR dédiée.
