<header class="print:hidden sticky top-0 z-40 border-b border-slate-200 dark:border-white/[0.06] bg-slate-50/70 dark:bg-canvas-dark/70 backdrop-blur-md">
    <twig:Navbar>
        <twig:Navbar:LogoText as="a" href="{{ path(constant('App\\Shared\\Domain\\Data\\Route::HOME').value) }}">
            Hugo Alliaume
        </twig:Navbar:LogoText>

        {{ knp_menu_render('main', {
            template: 'menu/main.html.twig',
        }) }}
    </twig:Navbar>
</header>
